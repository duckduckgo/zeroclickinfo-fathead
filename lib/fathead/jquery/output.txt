.add()	A			jQuery methods							Create a new jQuery object with elements added to the set of matched elements.<pre><code>.add(selector) → jQuery\n.add(elements) → jQuery\n.add(html) → jQuery\n.add(selection) → jQuery\n.add(selector, context) → jQuery</code></pre>	https://api.jquery.com/add/
.addBack()	A			jQuery methods							Add the previous set of elements on the stack to the current set, optionally filtered by a selector.<pre><code>.addBack(selector) → jQuery</code></pre>	https://api.jquery.com/addBack/
.addClass()	A			jQuery methods							Adds the specified class(es) to each element in the set of matched elements.<pre><code>.addClass(className) → jQuery\n.addClass(function, index, currentClassName) → jQuery</code></pre>	https://api.jquery.com/addClass/
.after()	A			jQuery methods							Insert content, specified by the parameter, after each element in the set of matched elements.<pre><code>.after(content, content) → jQuery\n.after(function, index) → jQuery\n.after(function-html, index, html) → jQuery</code></pre>	https://api.jquery.com/after/
.ajaxComplete()	A			jQuery methods							Register a handler to be called when Ajax requests complete. This is an AjaxEvent.<pre><code>.ajaxComplete(handler, event, jqXHR, ajaxOptions) → jQuery</code></pre>	https://api.jquery.com/ajaxComplete/
.ajaxError()	A			jQuery methods							Register a handler to be called when Ajax requests complete with an error. This is an Ajax Event.<pre><code>.ajaxError(handler, event, jqXHR, ajaxSettings, thrownError) → jQuery</code></pre>	https://api.jquery.com/ajaxError/
.ajaxSend()	A			jQuery methods							Attach a function to be executed before an Ajax request is sent. This is an Ajax Event.<pre><code>.ajaxSend(handler, event, jqXHR, ajaxOptions) → jQuery</code></pre>	https://api.jquery.com/ajaxSend/
.ajaxStart()	A			jQuery methods							Register a handler to be called when the first Ajax request begins. This is an Ajax Event.<pre><code>.ajaxStart(handler) → jQuery</code></pre>	https://api.jquery.com/ajaxStart/
.ajaxStop()	A			jQuery methods							Register a handler to be called when all Ajax requests have completed. This is an Ajax Event.<pre><code>.ajaxStop(handler) → jQuery</code></pre>	https://api.jquery.com/ajaxStop/
.ajaxSuccess()	A			jQuery methods							Attach a function to be executed whenever an Ajax request completes successfully. This is an Ajax Event.<pre><code>.ajaxSuccess(handler, event, jqXHR, ajaxOptions, data) → jQuery</code></pre>	https://api.jquery.com/ajaxSuccess/
All Selector ("*")	A			jQuery selectors							Selects all elements.<pre><code>All Selector ("*")</code></pre>	https://api.jquery.com/all-selector/
.andSelf()	A			jQuery methods							Add the previous set of elements on the stack to the current set.<pre><code>.andSelf() → jQuery</code></pre>	https://api.jquery.com/andSelf/
.animate()	A			jQuery methods							Perform a custom animation of a set of CSS properties.<pre><code>.animate(properties) → jQuery\n.animate(properties) → jQuery</code></pre>	https://api.jquery.com/animate/
:animated Selector	A			jQuery selectors							Select all elements that are in the progress of an animation at the time the selector is run.<pre><code>:animated Selector</code></pre>	https://api.jquery.com/animated-selector/
.append()	A			jQuery methods							Insert content, specified by the parameter, to the end of each element in the set of matched elements.<pre><code>.append(content, content) → jQuery\n.append(function, index, html) → jQuery</code></pre>	https://api.jquery.com/append/
.appendTo()	A			jQuery methods							Insert every element in the set of matched elements to the end of the target.<pre><code>.appendTo(target) → jQuery</code></pre>	https://api.jquery.com/appendTo/
.attr()	A			jQuery methods							Get the value of an attribute for the first element in the set of matched elements.Set one or more attributes for the set of matched elements.<pre><code>.attr(attributeName) → String\n.attr(attributeName, value) → String\n.attr(attributes) → String\n.attr(attributeName, function, index, attr) → String</code></pre>	https://api.jquery.com/attr/
Attribute Contains Prefix Selector [name|="value"]	A			jQuery selectors							Selects elements that have the specified attribute with a value either equal to a given string or starting with that string followed by a hyphen (-).<pre><code>Attribute Contains Prefix Selector [name|="value"]</code></pre>	https://api.jquery.com/attribute-contains-prefix-selector/
Attribute Contains Selector [name*="value"]	A			jQuery selectors							Selects elements that have the specified attribute with a value containing a given substring.<pre><code>Attribute Contains Selector [name*="value"]</code></pre>	https://api.jquery.com/attribute-contains-selector/
Attribute Contains Word Selector [name~="value"]	A			jQuery selectors							Selects elements that have the specified attribute with a value containing a given word, delimited by spaces.<pre><code>Attribute Contains Word Selector [name~="value"]</code></pre>	https://api.jquery.com/attribute-contains-word-selector/
Attribute Ends With Selector [name$="value"]	A			jQuery selectors							Selects elements that have the specified attribute with a value ending exactly with a given string. The comparison is case sensitive.<pre><code>Attribute Ends With Selector [name$="value"]</code></pre>	https://api.jquery.com/attribute-ends-with-selector/
Attribute Equals Selector [name="value"]	A			jQuery selectors							Selects elements that have the specified attribute with a value exactly equal to a certain value.<pre><code>Attribute Equals Selector [name="value"]</code></pre>	https://api.jquery.com/attribute-equals-selector/
Attribute Not Equal Selector [name!="value"]	A			jQuery selectors							Select elements that either don't have the specified attribute, or do have the specified attribute but not with a certain value.<pre><code>Attribute Not Equal Selector [name!="value"]</code></pre>	https://api.jquery.com/attribute-not-equal-selector/
Attribute Starts With Selector [name^="value"]	A			jQuery selectors							Selects elements that have the specified attribute with a value beginning exactly with a given string.<pre><code>Attribute Starts With Selector [name^="value"]</code></pre>	https://api.jquery.com/attribute-starts-with-selector/
.before()	A			jQuery methods							Insert content, specified by the parameter, before each element in the set of matched elements.<pre><code>.before(content, content) → jQuery\n.before(function, index) → jQuery\n.before(function-html, index, html) → jQuery</code></pre>	https://api.jquery.com/before/
.bind()	A			jQuery methods							Attach a handler to an event for the elements.<pre><code>.bind(eventType, eventData, handler, eventObject) → jQuery\n.bind(eventType, eventData, preventBubble) → jQuery\n.bind(events) → jQuery</code></pre>	https://api.jquery.com/bind/
.blur()	A			jQuery methods							Bind an event handler to the "blur" JavaScript event, or trigger that event on an element.<pre><code>.blur(handler, eventObject) → jQuery\n.blur(eventData, handler, eventObject) → jQuery\n.blur() → jQuery</code></pre>	https://api.jquery.com/blur/
:button Selector	A			jQuery selectors							Selects all button elements and elements of type button.<pre><code>:button Selector</code></pre>	https://api.jquery.com/button-selector/
callbacks.add()	A			jQuery methods							Add a callback or a collection of callbacks to a callback list.<pre><code>callbacks.add(callbacks) → Callbacks</code></pre>	https://api.jquery.com/callbacks.add/
callbacks.disable()	A			jQuery methods							Disable a callback list from doing anything more.<pre><code>callbacks.disable() → Callbacks</code></pre>	https://api.jquery.com/callbacks.disable/
callbacks.disabled()	A			jQuery methods							Determine if the callbacks list has been disabled.<pre><code>callbacks.disabled() → Boolean</code></pre>	https://api.jquery.com/callbacks.disabled/
callbacks.empty()	A			jQuery methods							Remove all of the callbacks from a list.<pre><code>callbacks.empty() → Callbacks</code></pre>	https://api.jquery.com/callbacks.empty/
callbacks.fire()	A			jQuery methods							Call all of the callbacks with the given arguments.<pre><code>callbacks.fire(arguments) → Callbacks</code></pre>	https://api.jquery.com/callbacks.fire/
callbacks.fired()	A			jQuery methods							Determine if the callbacks have already been called at least once.<pre><code>callbacks.fired() → Boolean</code></pre>	https://api.jquery.com/callbacks.fired/
callbacks.fireWith()	A			jQuery methods							Call all callbacks in a list with the given context and arguments.<pre><code>callbacks.fireWith(context, args) → Callbacks</code></pre>	https://api.jquery.com/callbacks.fireWith/
callbacks.has()	A			jQuery methods							Determine whether or not the list has any callbacks attached. If a callback is provided as an argument, determine whether it is in a list.<pre><code>callbacks.has(callback) → Boolean</code></pre>	https://api.jquery.com/callbacks.has/
callbacks.lock()	A			jQuery methods							Lock a callback list in its current state.<pre><code>callbacks.lock() → Callbacks</code></pre>	https://api.jquery.com/callbacks.lock/
callbacks.locked()	A			jQuery methods							Determine if the callbacks list has been locked.<pre><code>callbacks.locked() → Boolean</code></pre>	https://api.jquery.com/callbacks.locked/
callbacks.remove()	A			jQuery methods							Remove a callback or a collection of callbacks from a callback list.<pre><code>callbacks.remove(callbacks) → Callbacks</code></pre>	https://api.jquery.com/callbacks.remove/
.change()	A			jQuery methods							Bind an event handler to the "change" JavaScript event, or trigger that event on an element.<pre><code>.change(handler, eventObject) → jQuery\n.change(eventData, handler, eventObject) → jQuery\n.change() → jQuery</code></pre>	https://api.jquery.com/change/
:checkbox Selector	A			jQuery selectors							Selects all elements of type checkbox.<pre><code>:checkbox Selector</code></pre>	https://api.jquery.com/checkbox-selector/
:checked Selector	A			jQuery selectors							Matches all elements that are checked or selected.<pre><code>:checked Selector</code></pre>	https://api.jquery.com/checked-selector/
.children()	A			jQuery methods							Get the children of each element in the set of matched elements, optionally filtered by a selector.<pre><code>.children(selector) → jQuery</code></pre>	https://api.jquery.com/children/
Child Selector ("parent > child")	A			jQuery selectors							Selects all direct child elements specified by "child" of elements specified by "parent".<pre><code>Child Selector ("parent > child")</code></pre>	https://api.jquery.com/child-selector/
Class Selector (".class")	A			jQuery selectors							Selects all elements with the given class. <pre><code>Class Selector (".class")</code></pre>	https://api.jquery.com/class-selector/
.clearQueue()	A			jQuery methods							Remove from the queue all items that have not yet been run.<pre><code>.clearQueue(queueName) → jQuery</code></pre>	https://api.jquery.com/clearQueue/
.click()	A			jQuery methods							Bind an event handler to the "click" JavaScript event, or trigger that event on an element.<pre><code>.click(handler, eventObject) → jQuery\n.click(eventData, handler, eventObject) → jQuery\n.click() → jQuery</code></pre>	https://api.jquery.com/click/
.clone()	A			jQuery methods							Create a deep copy of the set of matched elements.<pre><code>.clone(withDataAndEvents) → jQuery\n.clone(withDataAndEvents, deepWithDataAndEvents) → jQuery</code></pre>	https://api.jquery.com/clone/
.closest()	A			jQuery methods							For each element in the set, get the first element that matches the selector by testing the element itself and traversing up through its ancestors in the DOM tree.Get an array of all the elements and selectors matched against the current element up through the DOM tree.<pre><code>.closest(selector) → jQuery\n.closest(selector, context) → jQuery\n.closest(selection) → jQuery\n.closest(element) → jQuery\n.closest(selectors, context) → jQuery</code></pre>	https://api.jquery.com/closest/
:contains() Selector	A			jQuery selectors							Select all elements that contain the specified text.<pre><code>:contains(text) →  Selector</code></pre>	https://api.jquery.com/contains-selector/
.contents()	A			jQuery methods							Get the children of each element in the set of matched elements, including text and comment nodes.<pre><code>.contents() → jQuery</code></pre>	https://api.jquery.com/contents/
.context	A			jQuery properties							The DOM node context originally passed to jQuery(); if none was passed then context will likely be the document.<pre><code>.context</code></pre>	https://api.jquery.com/context/
.contextmenu()	A			jQuery methods							Bind an event handler to the "contextmenu" JavaScript event, or trigger that event on an element.<pre><code>.contextmenu(handler, eventObject) → jQuery\n.contextmenu(eventData, handler, eventObject) → jQuery\n.contextmenu() → jQuery</code></pre>	https://api.jquery.com/contextmenu/
.css()	A			jQuery methods							Get the computed style properties for the first element in the set of matched elements.Set one or more CSS properties for the set of matched elements.<pre><code>.css(propertyName) → String\n.css(propertyNames) → String\n.css(propertyName, value) → String\n.css(propertyName, function, index, value) → String\n.css(properties) → String</code></pre>	https://api.jquery.com/css/
.data()	A			jQuery methods							Store arbitrary data associated with the matched elements.Return the value at the named data store for the first element in the jQuery collection, as set by data(name, value) or by an HTML5 data-* attribute.<pre><code>.data(key, value) → jQuery\n.data(obj) → jQuery\n.data(key) → jQuery\n.data() → jQuery</code></pre>	https://api.jquery.com/data/
.dblclick()	A			jQuery methods							Bind an event handler to the "dblclick" JavaScript event, or trigger that event on an element.<pre><code>.dblclick(handler, eventObject) → jQuery\n.dblclick(eventData, handler, eventObject) → jQuery\n.dblclick() → jQuery</code></pre>	https://api.jquery.com/dblclick/
deferred.always()	A			jQuery methods							 Add handlers to be called when the Deferred object is either resolved or rejected. <pre><code>deferred.always(alwaysCallbacks, alwaysCallbacks) → Deferred</code></pre>	https://api.jquery.com/deferred.always/
deferred.done()	A			jQuery methods							 Add handlers to be called when the Deferred object is resolved. <pre><code>deferred.done(doneCallbacks, doneCallbacks) → Deferred</code></pre>	https://api.jquery.com/deferred.done/
deferred.fail()	A			jQuery methods							 Add handlers to be called when the Deferred object is rejected. <pre><code>deferred.fail(failCallbacks, failCallbacks) → Deferred</code></pre>	https://api.jquery.com/deferred.fail/
deferred.isRejected()	A			jQuery methods							 Determine whether a Deferred object has been rejected. <pre><code>deferred.isRejected() → Boolean</code></pre>	https://api.jquery.com/deferred.isRejected/
deferred.isResolved()	A			jQuery methods							 Determine whether a Deferred object has been resolved. <pre><code>deferred.isResolved() → Boolean</code></pre>	https://api.jquery.com/deferred.isResolved/
deferred.notify()	A			jQuery methods							 Call the progressCallbacks on a Deferred object with the given args. <pre><code>deferred.notify(args) → Deferred</code></pre>	https://api.jquery.com/deferred.notify/
deferred.notifyWith()	A			jQuery methods							 Call the progressCallbacks on a Deferred object with the given context and args. <pre><code>deferred.notifyWith(context, args) → Deferred</code></pre>	https://api.jquery.com/deferred.notifyWith/
deferred.pipe()	A			jQuery methods							 Utility method to filter and/or chain Deferreds.  <pre><code>deferred.pipe(doneFilter, failFilter) → Promise\ndeferred.pipe(doneFilter, failFilter, progressFilter) → Promise</code></pre>	https://api.jquery.com/deferred.pipe/
deferred.progress()	A			jQuery methods							 Add handlers to be called when the Deferred object generates progress notifications.<pre><code>deferred.progress(progressCallbacks, progressCallbacks) → Deferred</code></pre>	https://api.jquery.com/deferred.progress/
deferred.promise()	A			jQuery methods							 Return a Deferred's Promise object. <pre><code>deferred.promise(target) → Promise</code></pre>	https://api.jquery.com/deferred.promise/
deferred.reject()	A			jQuery methods							 Reject a Deferred object and call any failCallbacks with the given args. <pre><code>deferred.reject(args) → Deferred</code></pre>	https://api.jquery.com/deferred.reject/
deferred.rejectWith()	A			jQuery methods							 Reject a Deferred object and call any failCallbacks with the given context and args. <pre><code>deferred.rejectWith(context, args) → Deferred</code></pre>	https://api.jquery.com/deferred.rejectWith/
deferred.resolve()	A			jQuery methods							 Resolve a Deferred object and call any doneCallbacks with the given args. <pre><code>deferred.resolve(args) → Deferred</code></pre>	https://api.jquery.com/deferred.resolve/
deferred.resolveWith()	A			jQuery methods							 Resolve a Deferred object and call any doneCallbacks with the given context and args. <pre><code>deferred.resolveWith(context, args) → Deferred</code></pre>	https://api.jquery.com/deferred.resolveWith/
deferred.state()	A			jQuery methods							Determine the current state of a Deferred object. <pre><code>deferred.state() → String</code></pre>	https://api.jquery.com/deferred.state/
deferred.then()	A			jQuery methods							Add handlers to be called when the Deferred object is resolved, rejected, or still in progress. <pre><code>deferred.then(doneFilter, failFilter, progressFilter) → Promise\ndeferred.then(doneCallbacks, failCallbacks) → Promise\ndeferred.then(doneCallbacks, failCallbacks, progressCallbacks) → Promise</code></pre>	https://api.jquery.com/deferred.then/
.delay()	A			jQuery methods							Set a timer to delay execution of subsequent items in the queue.<pre><code>.delay(duration, queueName) → jQuery</code></pre>	https://api.jquery.com/delay/
.delegate()	A			jQuery methods							Attach a handler to one or more events for all elements that match the selector, now or in the future, based on a specific set of root elements.<pre><code>.delegate(selector, eventType, handler, eventObject) → jQuery\n.delegate(selector, eventType, eventData, handler, eventObject) → jQuery\n.delegate(selector, events) → jQuery</code></pre>	https://api.jquery.com/delegate/
.dequeue()	A			jQuery methods							Execute the next function on the queue for the matched elements.<pre><code>.dequeue(queueName) → jQuery</code></pre>	https://api.jquery.com/dequeue/
Descendant Selector ("ancestor descendant")	A			jQuery selectors							Selects all elements that are descendants of a given ancestor.<pre><code>Descendant Selector ("ancestor descendant")</code></pre>	https://api.jquery.com/descendant-selector/
.detach()	A			jQuery methods							Remove the set of matched elements from the DOM.<pre><code>.detach(selector) → jQuery</code></pre>	https://api.jquery.com/detach/
.die()	A			jQuery methods							Remove event handlers previously attached using .live() from the elements.<pre><code>.die() → jQuery\n.die(eventType, handler) → jQuery\n.die(events) → jQuery</code></pre>	https://api.jquery.com/die/
:disabled Selector	A			jQuery selectors							Selects all elements that are disabled.<pre><code>:disabled Selector</code></pre>	https://api.jquery.com/disabled-selector/
.each()	A			jQuery methods							Iterate over a jQuery object, executing a function for each matched element. <pre><code>.each(function, index, element) → jQuery</code></pre>	https://api.jquery.com/each/
Element Selector ("element")	A			jQuery selectors							Selects all elements with the given tag name.<pre><code>Element Selector ("element")</code></pre>	https://api.jquery.com/element-selector/
.empty()	A			jQuery methods							Remove all child nodes of the set of matched elements from the DOM.<pre><code>.empty() → jQuery</code></pre>	https://api.jquery.com/empty/
:empty Selector	A			jQuery selectors							Select all elements that have no children (including text nodes).<pre><code>:empty Selector</code></pre>	https://api.jquery.com/empty-selector/
:enabled Selector	A			jQuery selectors							Selects all elements that are enabled.<pre><code>:enabled Selector</code></pre>	https://api.jquery.com/enabled-selector/
.end()	A			jQuery methods							End the most recent filtering operation in the current chain and return the set of matched elements to its previous state.<pre><code>.end() → jQuery</code></pre>	https://api.jquery.com/end/
.eq()	A			jQuery methods							Reduce the set of matched elements to the one at the specified index.<pre><code>.eq(index) → jQuery\n.eq(indexFromEnd) → jQuery</code></pre>	https://api.jquery.com/eq/
:eq() Selector	A			jQuery selectors							Select the element at index n within the matched set.<pre><code>:eq(index) →  Selector\n:eq(indexFromEnd) →  Selector</code></pre>	https://api.jquery.com/eq-selector/
.error()	A			jQuery methods							Bind an event handler to the "error" JavaScript event.<pre><code>.error(handler, eventObject) → jQuery\n.error(eventData, handler, eventObject) → jQuery</code></pre>	https://api.jquery.com/error/
:even Selector	A			jQuery selectors							Selects even elements, zero-indexed.  See also odd.<pre><code>:even Selector</code></pre>	https://api.jquery.com/even-selector/
event.currentTarget	A			jQuery properties							 The current DOM element within the event bubbling phase.  <pre><code>event.currentTarget</code></pre>	https://api.jquery.com/event.currentTarget/
event.data	A			jQuery properties							An optional object of data passed to an event method when the current executing handler is bound.  <pre><code>event.data</code></pre>	https://api.jquery.com/event.data/
event.delegateTarget	A			jQuery properties							The element where the currently-called jQuery event handler was attached.<pre><code>event.delegateTarget</code></pre>	https://api.jquery.com/event.delegateTarget/
event.isDefaultPrevented()	A			jQuery methods							Returns whether event.preventDefault() was ever called on this event object. <pre><code>event.isDefaultPrevented() → Boolean</code></pre>	https://api.jquery.com/event.isDefaultPrevented/
event.isImmediatePropagationStopped()	A			jQuery methods							  Returns whether event.stopImmediatePropagation() was ever called on this event object. <pre><code>event.isImmediatePropagationStopped() → Boolean</code></pre>	https://api.jquery.com/event.isImmediatePropagationStopped/
event.isPropagationStopped()	A			jQuery methods							  Returns whether event.stopPropagation() was ever called on this event object. <pre><code>event.isPropagationStopped() → Boolean</code></pre>	https://api.jquery.com/event.isPropagationStopped/
event.metaKey	A			jQuery properties							Indicates whether the META key was pressed when the event fired.<pre><code>event.metaKey</code></pre>	https://api.jquery.com/event.metaKey/
event.namespace	A			jQuery properties							The namespace specified when the event was triggered.<pre><code>event.namespace</code></pre>	https://api.jquery.com/event.namespace/
event.pageX	A			jQuery properties							The mouse position relative to the left edge of the document.<pre><code>event.pageX</code></pre>	https://api.jquery.com/event.pageX/
event.pageY	A			jQuery properties							The mouse position relative to the top edge of the document.<pre><code>event.pageY</code></pre>	https://api.jquery.com/event.pageY/
event.preventDefault()	A			jQuery methods							If this method is called, the default action of the event will not be triggered.<pre><code>event.preventDefault() → undefined</code></pre>	https://api.jquery.com/event.preventDefault/
event.relatedTarget	A			jQuery properties							The other DOM element involved in the event, if any.<pre><code>event.relatedTarget</code></pre>	https://api.jquery.com/event.relatedTarget/
event.result	A			jQuery properties							The last value returned by an event handler that was triggered by this event, unless the value was undefined.<pre><code>event.result</code></pre>	https://api.jquery.com/event.result/
event.stopImmediatePropagation()	A			jQuery methods							Keeps the rest of the handlers from being executed and prevents the event from bubbling up the DOM tree.<pre><code>event.stopImmediatePropagation() → </code></pre>	https://api.jquery.com/event.stopImmediatePropagation/
event.stopPropagation()	A			jQuery methods							Prevents the event from bubbling up the DOM tree, preventing any parent handlers from being notified of the event.<pre><code>event.stopPropagation() → </code></pre>	https://api.jquery.com/event.stopPropagation/
event.target	A			jQuery properties							 The DOM element that initiated the event.  <pre><code>event.target</code></pre>	https://api.jquery.com/event.target/
event.timeStamp	A			jQuery properties							The difference in milliseconds between the time the browser created the event and January 1, 1970.<pre><code>event.timeStamp</code></pre>	https://api.jquery.com/event.timeStamp/
event.type	A			jQuery properties							Describes the nature of the event.<pre><code>event.type</code></pre>	https://api.jquery.com/event.type/
event.which	A			jQuery properties							For key or mouse events, this property indicates the specific key or button that was pressed.<pre><code>event.which</code></pre>	https://api.jquery.com/event.which/
.fadeIn()	A			jQuery methods							Display the matched elements by fading them to opaque.<pre><code>.fadeIn() → jQuery\n.fadeIn() → jQuery\n.fadeIn() → jQuery</code></pre>	https://api.jquery.com/fadeIn/
.fadeOut()	A			jQuery methods							Hide the matched elements by fading them to transparent.<pre><code>.fadeOut() → jQuery\n.fadeOut() → jQuery\n.fadeOut() → jQuery</code></pre>	https://api.jquery.com/fadeOut/
.fadeTo()	A			jQuery methods							Adjust the opacity of the matched elements.<pre><code>.fadeTo(duration, opacity, complete) → jQuery\n.fadeTo(duration, opacity, easing, complete) → jQuery</code></pre>	https://api.jquery.com/fadeTo/
.fadeToggle()	A			jQuery methods							Display or hide the matched elements by animating their opacity.<pre><code>.fadeToggle() → jQuery\n.fadeToggle() → jQuery</code></pre>	https://api.jquery.com/fadeToggle/
:file Selector	A			jQuery selectors							Selects all elements of type file.<pre><code>:file Selector</code></pre>	https://api.jquery.com/file-selector/
.filter()	A			jQuery methods							Reduce the set of matched elements to those that match the selector or pass the function's test. <pre><code>.filter(selector) → jQuery\n.filter(function, index, element) → jQuery\n.filter(elements) → jQuery\n.filter(selection) → jQuery</code></pre>	https://api.jquery.com/filter/
.find()	A			jQuery methods							Get the descendants of each element in the current set of matched elements, filtered by a selector, jQuery object, or element.<pre><code>.find(selector) → jQuery\n.find(element) → jQuery</code></pre>	https://api.jquery.com/find/
.finish()	A			jQuery methods							Stop the currently-running animation, remove all queued animations, and complete all animations for the matched elements.<pre><code>.finish(queue) → jQuery</code></pre>	https://api.jquery.com/finish/
.first()	A			jQuery methods							Reduce the set of matched elements to the first in the set.<pre><code>.first() → jQuery</code></pre>	https://api.jquery.com/first/
:first-child Selector	A			jQuery selectors							Selects all elements that are the first child of their parent.<pre><code>:first-child Selector</code></pre>	https://api.jquery.com/first-child-selector/
:first-of-type Selector	A			jQuery selectors							Selects all elements that are the first among siblings of the same element name.<pre><code>:first-of-type Selector</code></pre>	https://api.jquery.com/first-of-type-selector/
:first Selector	A			jQuery selectors							Selects the first matched DOM element.<pre><code>:first Selector</code></pre>	https://api.jquery.com/first-selector/
.focus()	A			jQuery methods							Bind an event handler to the "focus" JavaScript event, or trigger that event on an element.<pre><code>.focus(handler, eventObject) → jQuery\n.focus(eventData, handler, eventObject) → jQuery\n.focus() → jQuery</code></pre>	https://api.jquery.com/focus/
.focusin()	A			jQuery methods							Bind an event handler to the "focusin" event.<pre><code>.focusin(handler, eventObject) → jQuery\n.focusin(eventData, handler, eventObject) → jQuery\n.focusin() → jQuery</code></pre>	https://api.jquery.com/focusin/
.focusout()	A			jQuery methods							Bind an event handler to the "focusout" JavaScript event.<pre><code>.focusout(handler, eventObject) → jQuery\n.focusout(eventData, handler, eventObject) → jQuery\n.focusout() → jQuery</code></pre>	https://api.jquery.com/focusout/
:focus Selector	A			jQuery selectors							Selects element if it is currently focused.<pre><code>:focus Selector</code></pre>	https://api.jquery.com/focus-selector/
.get().get()	A			jQuery methods							Retrieve one of the elements matched by the jQuery object.Retrieve the elements matched by the jQuery object.<pre><code>.get(index) → Element.get()\n.get() → Element.get()</code></pre>	https://api.jquery.com/get/
:gt() Selector	A			jQuery selectors							Select all elements at an index greater than index within the matched set.<pre><code>:gt(index) →  Selector\n:gt(indexFromEnd) →  Selector</code></pre>	https://api.jquery.com/gt-selector/
.has()	A			jQuery methods							Reduce the set of matched elements to those that have a descendant that matches the selector or DOM element.<pre><code>.has(selector) → jQuery\n.has(contained) → jQuery</code></pre>	https://api.jquery.com/has/
Has Attribute Selector [name]	A			jQuery selectors							Selects elements that have the specified attribute, with any value. <pre><code>Has Attribute Selector [name]</code></pre>	https://api.jquery.com/has-attribute-selector/
.hasClass()	A			jQuery methods							Determine whether any of the matched elements are assigned the given class.<pre><code>.hasClass(className) → Boolean</code></pre>	https://api.jquery.com/hasClass/
:has() Selector	A			jQuery selectors							Selects elements which contain at least one element that matches the specified selector.<pre><code>:has(selector) →  Selector</code></pre>	https://api.jquery.com/has-selector/
:header Selector	A			jQuery selectors							Selects all elements that are headers, like h1, h2, h3 and so on.<pre><code>:header Selector</code></pre>	https://api.jquery.com/header-selector/
.height()	A			jQuery methods							Get the current computed height for the first element in the set of matched elements.Set the CSS height of every matched element.<pre><code>.height() → Number\n.height(value) → Number\n.height(function, index, height) → Number</code></pre>	https://api.jquery.com/height/
:hidden Selector	A			jQuery selectors							Selects all elements that are hidden.<pre><code>:hidden Selector</code></pre>	https://api.jquery.com/hidden-selector/
.hide()	A			jQuery methods							Hide the matched elements.<pre><code>.hide() → jQuery\n.hide() → jQuery\n.hide() → jQuery\n.hide(duration) → jQuery</code></pre>	https://api.jquery.com/hide/
.hover()	A			jQuery methods							Bind two handlers to the matched elements, to be executed when the mouse pointer enters and leaves the elements.Bind a single handler to the matched elements, to be executed when the mouse pointer enters or leaves the elements.<pre><code>.hover(handlerIn, eventObject, handlerOut, eventObject) → jQuery\n.hover(handlerInOut, eventObject) → jQuery</code></pre>	https://api.jquery.com/hover/
.html()	A			jQuery methods							Get the HTML contents of the first element in the set of matched elements.Set the HTML contents of each element in the set of matched elements.<pre><code>.html() → String\n.html(htmlString) → String\n.html(function, index, oldhtml) → String</code></pre>	https://api.jquery.com/html/
ID Selector ("#id")	A			jQuery selectors							Selects a single element with the given id attribute. <pre><code>ID Selector ("#id")</code></pre>	https://api.jquery.com/id-selector/
:image Selector	A			jQuery selectors							Selects all elements of type image.<pre><code>:image Selector</code></pre>	https://api.jquery.com/image-selector/
.index()	A			jQuery methods							Search for a given element from among the matched elements.<pre><code>.index() → Integer\n.index(selector) → Integer\n.index(element) → Integer</code></pre>	https://api.jquery.com/index/
.innerHeight()	A			jQuery methods							Get the current computed height for the first element in the set of matched elements, including padding but not border.Set the CSS inner height of each element in the set of matched elements.<pre><code>.innerHeight() → Number\n.innerHeight(value) → Number\n.innerHeight(function, index, height) → Number</code></pre>	https://api.jquery.com/innerHeight/
.innerWidth()	A			jQuery methods							Get the current computed inner width for the first element in the set of matched elements, including padding but not border.Set the CSS inner width of each element in the set of matched elements.<pre><code>.innerWidth() → Number\n.innerWidth(value) → Number\n.innerWidth(function, index, width) → Number</code></pre>	https://api.jquery.com/innerWidth/
:input Selector	A			jQuery selectors							Selects all input, textarea, select and button elements.<pre><code>:input Selector</code></pre>	https://api.jquery.com/input-selector/
.insertAfter()	A			jQuery methods							Insert every element in the set of matched elements after the target.<pre><code>.insertAfter(target) → jQuery</code></pre>	https://api.jquery.com/insertAfter/
.insertBefore()	A			jQuery methods							Insert every element in the set of matched elements before the target.<pre><code>.insertBefore(target) → jQuery</code></pre>	https://api.jquery.com/insertBefore/
.is()	A			jQuery methods							Check the current matched set of elements against a selector, element, or jQuery object and return true if at least one of these elements matches the given arguments.<pre><code>.is(selector) → Boolean\n.is(function, index, element) → Boolean\n.is(selection) → Boolean\n.is(elements) → Boolean</code></pre>	https://api.jquery.com/is/
jQuery.ajax()	A			jQuery methods							Perform an asynchronous HTTP (Ajax) request.<pre><code>jQuery.ajax(url, settings) → jqXHR\njQuery.ajax(settings, jqXHR, settings, jqXHR, textStatus, data, type, jqXHR, textStatus, errorThrown, data, textStatus, jqXHR) → jqXHR</code></pre>	https://api.jquery.com/jQuery.ajax/
jQuery.ajaxPrefilter()	A			jQuery methods							Handle custom Ajax options or modify existing options before each request is sent and before they are processed by $.ajax().<pre><code>jQuery.ajaxPrefilter(dataTypes, handler, options, originalOptions, jqXHR) → undefined</code></pre>	https://api.jquery.com/jQuery.ajaxPrefilter/
jQuery.ajaxSetup()	A			jQuery methods							Set default values for future Ajax requests. Its use is not recommended.<pre><code>jQuery.ajaxSetup(options) → </code></pre>	https://api.jquery.com/jQuery.ajaxSetup/
jQuery.ajaxTransport()	A			jQuery methods							Creates an object that handles the actual transmission of Ajax data.<pre><code>jQuery.ajaxTransport(dataType, handler, options, originalOptions, jqXHR) → undefined</code></pre>	https://api.jquery.com/jQuery.ajaxTransport/
jQuery()	A			jQuery methods							Accepts a string containing a CSS selector which is then used to match a set of elements.Creates DOM elements on the fly from the provided string of raw HTML.Binds a function to be executed when the DOM has finished loading.<pre><code>jQuery(selector, context) → jQuery\njQuery(element) → jQuery\njQuery(elementArray) → jQuery\njQuery(object) → jQuery\njQuery(selection) → jQuery\njQuery() → jQuery\njQuery(html, ownerDocument) → jQuery\njQuery(html, attributes) → jQuery\njQuery(callback) → jQuery</code></pre>	https://api.jquery.com/jQuery/
.jquery	A			jQuery properties							A string containing the jQuery version number.<pre><code>.jquery</code></pre>	https://api.jquery.com/jquery-2/
jQuery.boxModel	A			jQuery properties							States if the current page, in the user's browser, is being rendered using the W3C CSS Box Model. This property was removed in jQuery 1.8. Please try to use feature detection instead.<pre><code>jQuery.boxModel</code></pre>	https://api.jquery.com/jQuery.boxModel/
jQuery.browser	A			jQuery properties							Contains flags for the useragent, read from navigator.userAgent. This property was removed in jQuery 1.9 and is available only through the jQuery.migrate plugin. Please try to use feature detection instead.The version number of the rendering engine for the user's browser. This property was removed in jQuery 1.9 and is available only through the jQuery.migrate plugin.<pre><code>jQuery.browser\njQuery.browser</code></pre>	https://api.jquery.com/jQuery.browser/
jQuery.Callbacks()	A			jQuery methods							A multi-purpose callbacks list object that provides a powerful way to manage callback lists.<pre><code>jQuery.Callbacks(flags) → Callbacks</code></pre>	https://api.jquery.com/jQuery.Callbacks/
jQuery.contains()	A			jQuery methods							Check to see if a DOM element is a descendant of another DOM element.<pre><code>jQuery.contains(container, contained) → Boolean</code></pre>	https://api.jquery.com/jQuery.contains/
jQuery.cssHooks	A			jQuery properties							Hook directly into jQuery to override how particular CSS properties are retrieved or set, normalize CSS property naming, or create custom properties.<pre><code>jQuery.cssHooks</code></pre>	https://api.jquery.com/jQuery.cssHooks/
jQuery.cssNumber	A			jQuery properties							An object containing all CSS properties that may be used without a unit. The .css() method uses this object to see if it may append px to unitless values.<pre><code>jQuery.cssNumber</code></pre>	https://api.jquery.com/jQuery.cssNumber/
jQuery.data()	A			jQuery methods							Store arbitrary data associated with the specified element. Returns the value that was set.Returns value at named data store for the element, as set by jQuery.data(element, name, value), or the full data store for the element.<pre><code>jQuery.data(element, key, value) → Object\njQuery.data(element, key) → Object\njQuery.data(element) → Object</code></pre>	https://api.jquery.com/jQuery.data/
jQuery.Deferred()	A			jQuery methods							 A factory function that returns a chainable utility object with methods to register multiple callbacks into callback queues, invoke callback queues, and relay the success or failure state of any synchronous or asynchronous function.<pre><code>jQuery.Deferred(beforeStart, deferred) → Deferred</code></pre>	https://api.jquery.com/jQuery.Deferred/
jQuery.dequeue()	A			jQuery methods							Execute the next function on the queue for the matched element.<pre><code>jQuery.dequeue(element, queueName) → undefined</code></pre>	https://api.jquery.com/jQuery.dequeue/
jQuery.each()	A			jQuery methods							A generic iterator function, which can be used to seamlessly iterate over both objects and arrays. Arrays and array-like objects with a length property (such as a function's arguments object) are iterated by numeric index, from 0 to length-1. Other objects are iterated via their named properties.<pre><code>jQuery.each(array, callback, indexInArray, value) → Object\njQuery.each(object, callback, propertyName, valueOfProperty) → Object</code></pre>	https://api.jquery.com/jQuery.each/
jQuery.error()	A			jQuery methods							Takes a string and throws an exception containing it.<pre><code>jQuery.error(message) → </code></pre>	https://api.jquery.com/jQuery.error/
jQuery.escapeSelector()	A			jQuery methods							Escapes any character that has a special meaning in a CSS selector.<pre><code>jQuery.escapeSelector(selector) → Selector</code></pre>	https://api.jquery.com/jQuery.escapeSelector/
jQuery.extend()	A			jQuery methods							Merge the contents of two or more objects together into the first object.<pre><code>jQuery.extend(target, object1, objectN) → Object\njQuery.extend(deep, target, object1, objectN) → Object</code></pre>	https://api.jquery.com/jQuery.extend/
jQuery.fn.extend()	A			jQuery methods							Merge the contents of an object onto the jQuery prototype to provide new jQuery instance methods.<pre><code>jQuery.fn.extend(object) → Object</code></pre>	https://api.jquery.com/jQuery.fn.extend/
jQuery.fx.interval	A			jQuery properties							The rate (in milliseconds) at which animations fire.<pre><code>jQuery.fx.interval</code></pre>	https://api.jquery.com/jQuery.fx.interval/
jQuery.fx.off	A			jQuery properties							Globally disable all animations.<pre><code>jQuery.fx.off</code></pre>	https://api.jquery.com/jQuery.fx.off/
jQuery.get()	A			jQuery methods							Load data from the server using a HTTP GET request.<pre><code>jQuery.get(url, data, success, data, textStatus, jqXHR, dataType) → jqXHR\njQuery.get(settings) → jqXHR</code></pre>	https://api.jquery.com/jQuery.get/
jQuery.getJSON()	A			jQuery methods							Load JSON-encoded data from the server using a GET HTTP request.<pre><code>jQuery.getJSON(url, data, success, data, textStatus, jqXHR) → jqXHR</code></pre>	https://api.jquery.com/jQuery.getJSON/
jQuery.getScript()	A			jQuery methods							Load a JavaScript file from the server using a GET HTTP request, then execute it.<pre><code>jQuery.getScript(url, success, script, textStatus, jqXHR) → jqXHR</code></pre>	https://api.jquery.com/jQuery.getScript/
jQuery.globalEval()	A			jQuery methods							Execute some JavaScript code globally.<pre><code>jQuery.globalEval(code) → </code></pre>	https://api.jquery.com/jQuery.globalEval/
jQuery.grep()	A			jQuery methods							Finds the elements of an array which satisfy a filter function. The original array is not affected.<pre><code>jQuery.grep(array, function, elementOfArray, indexInArray, invert) → Array</code></pre>	https://api.jquery.com/jQuery.grep/
jQuery.hasData()	A			jQuery methods							Determine whether an element has any jQuery data associated with it.<pre><code>jQuery.hasData(element) → Boolean</code></pre>	https://api.jquery.com/jQuery.hasData/
jQuery.holdReady()	A			jQuery methods							Holds or releases the execution of jQuery's ready event.<pre><code>jQuery.holdReady(hold) → undefined</code></pre>	https://api.jquery.com/jQuery.holdReady/
jQuery.htmlPrefilter()	A			jQuery methods							Modify and filter HTML strings passed through jQuery manipulation methods.<pre><code>jQuery.htmlPrefilter(html) → String</code></pre>	https://api.jquery.com/jQuery.htmlPrefilter/
jQuery.inArray()	A			jQuery methods							Search for a specified value within an array and return its index (or -1 if not found).<pre><code>jQuery.inArray(value, array, fromIndex) → Number</code></pre>	https://api.jquery.com/jQuery.inArray/
jQuery.isArray()	A			jQuery methods							Determine whether the argument is an array.<pre><code>jQuery.isArray(obj) → boolean</code></pre>	https://api.jquery.com/jQuery.isArray/
jQuery.isEmptyObject()	A			jQuery methods							Check to see if an object is empty (contains no enumerable properties).<pre><code>jQuery.isEmptyObject(object) → Boolean</code></pre>	https://api.jquery.com/jQuery.isEmptyObject/
jQuery.isFunction()	A			jQuery methods							Determine if the argument passed is a JavaScript function object. <pre><code>jQuery.isFunction(obj) → boolean</code></pre>	https://api.jquery.com/jQuery.isFunction/
jQuery.isNumeric()	A			jQuery methods							Determines whether its argument represents a JavaScript number.<pre><code>jQuery.isNumeric(value) → Boolean</code></pre>	https://api.jquery.com/jQuery.isNumeric/
jQuery.isPlainObject()	A			jQuery methods							Check to see if an object is a plain object (created using "{}" or "new Object").<pre><code>jQuery.isPlainObject(object) → Boolean</code></pre>	https://api.jquery.com/jQuery.isPlainObject/
jQuery.isWindow()	A			jQuery methods							Determine whether the argument is a window.<pre><code>jQuery.isWindow(obj) → boolean</code></pre>	https://api.jquery.com/jQuery.isWindow/
jQuery.isXMLDoc()	A			jQuery methods							Check to see if a DOM node is within an XML document (or is an XML document).<pre><code>jQuery.isXMLDoc(node) → Boolean</code></pre>	https://api.jquery.com/jQuery.isXMLDoc/
jQuery.makeArray()	A			jQuery methods							Convert an array-like object into a true JavaScript array.<pre><code>jQuery.makeArray(obj) → Array</code></pre>	https://api.jquery.com/jQuery.makeArray/
jQuery.map()	A			jQuery methods							Translate all items in an array or object to new array of items.<pre><code>jQuery.map(array, callback, elementOfArray, indexInArray) → Array\njQuery.map(object, callback, propertyOfObject, key) → Array</code></pre>	https://api.jquery.com/jQuery.map/
jQuery.merge()	A			jQuery methods							Merge the contents of two arrays together into the first array. <pre><code>jQuery.merge(first, second) → Array</code></pre>	https://api.jquery.com/jQuery.merge/
jQuery.noConflict()	A			jQuery methods							Relinquish jQuery's control of the $ variable.<pre><code>jQuery.noConflict(removeAll) → Object</code></pre>	https://api.jquery.com/jQuery.noConflict/
jQuery.noop()	A			jQuery methods							An empty function.<pre><code>jQuery.noop() → undefined</code></pre>	https://api.jquery.com/jQuery.noop/
jQuery.now()	A			jQuery methods							Return a number representing the current time.<pre><code>jQuery.now() → Number</code></pre>	https://api.jquery.com/jQuery.now/
jQuery.param()	A			jQuery methods							Create a serialized representation of an array, a plain object, or a jQuery object suitable for use in a URL query string or Ajax request. In case a jQuery object is passed, it should contain input elements with name/value properties.<pre><code>jQuery.param(obj) → String\njQuery.param(obj, traditional) → String</code></pre>	https://api.jquery.com/jQuery.param/
jQuery.parseHTML()	A			jQuery methods							Parses a string into an array of DOM nodes.<pre><code>jQuery.parseHTML(data, context, keepScripts) → Array</code></pre>	https://api.jquery.com/jQuery.parseHTML/
jQuery.parseJSON()	A			jQuery methods							Takes a well-formed JSON string and returns the resulting JavaScript value.<pre><code>jQuery.parseJSON(json) → </code></pre>	https://api.jquery.com/jQuery.parseJSON/
jQuery.parseXML()	A			jQuery methods							Parses a string into an XML document.<pre><code>jQuery.parseXML(data) → XMLDocument</code></pre>	https://api.jquery.com/jQuery.parseXML/
jQuery.post()	A			jQuery methods							Load data from the server using a HTTP POST request.<pre><code>jQuery.post(url, data, success, data, textStatus, jqXHR, dataType) → jqXHR\njQuery.post(settings) → jqXHR</code></pre>	https://api.jquery.com/jQuery.post/
jQuery.proxy()	A			jQuery methods							Takes a function and returns a new one that will always have a particular context.<pre><code>jQuery.proxy(function, context) → Function\njQuery.proxy(context, name) → Function\njQuery.proxy(function, context, additionalArguments) → Function\njQuery.proxy(context, name, additionalArguments) → Function</code></pre>	https://api.jquery.com/jQuery.proxy/
jQuery.queue()	A			jQuery methods							Show the queue of functions to be executed on the matched element.Manipulate the queue of functions to be executed on the matched element.<pre><code>jQuery.queue(element, queueName) → Array\njQuery.queue(element, queueName, newQueue) → Array\njQuery.queue(element, queueName, callback) → Array</code></pre>	https://api.jquery.com/jQuery.queue/
jQuery.readyException()	A			jQuery methods							Handles errors thrown synchronously in functions wrapped in jQuery().<pre><code>jQuery.readyException(error) → Selector</code></pre>	https://api.jquery.com/jQuery.readyException/
jQuery.removeData()	A			jQuery methods							Remove a previously-stored piece of data.<pre><code>jQuery.removeData(element, name) → jQuery</code></pre>	https://api.jquery.com/jQuery.removeData/
jQuery.speed	A			jQuery methods							Creates an object containing a set of properties ready to be used in the definition of custom animations.<pre><code>jQuery.speed\njQuery.speed\njQuery.speed</code></pre>	https://api.jquery.com/jQuery.speed/
jQuery.sub()	A			jQuery methods							Creates a new copy of jQuery whose properties and methods can be modified without affecting the original jQuery object.<pre><code>jQuery.sub() → jQuery</code></pre>	https://api.jquery.com/jQuery.sub/
jQuery.support	A			jQuery properties							A collection of properties that represent the presence of different browser features or bugs. Intended for jQuery's internal use; specific properties may be removed when they are no longer needed internally to improve page startup performance. For your own project's feature-detection needs, we strongly recommend the use of an external library such as Modernizr instead of dependency on properties in jQuery.support.<pre><code>jQuery.support</code></pre>	https://api.jquery.com/jQuery.support/
jQuery.trim()	A			jQuery methods							Remove the whitespace from the beginning and end of a string.<pre><code>jQuery.trim(str) → String</code></pre>	https://api.jquery.com/jQuery.trim/
jQuery.type()	A			jQuery methods							Determine the internal JavaScript [[Class]] of an object.<pre><code>jQuery.type(obj) → String</code></pre>	https://api.jquery.com/jQuery.type/
jQuery.unique()	A			jQuery methods							Sorts an array of DOM elements, in place, with the duplicates removed. Note that this only works on arrays of DOM elements, not strings or numbers.<pre><code>jQuery.unique(array) → Array</code></pre>	https://api.jquery.com/jQuery.unique/
jQuery.uniqueSort()	A			jQuery methods							Sorts an array of DOM elements, in place, with the duplicates removed. Note that this only works on arrays of DOM elements, not strings or numbers.<pre><code>jQuery.uniqueSort(array) → Array</code></pre>	https://api.jquery.com/jQuery.uniqueSort/
jQuery.when()	A			jQuery methods							Provides a way to execute callback functions based on zero or more objects, usually Deferred objects that represent asynchronous events.<pre><code>jQuery.when(deferreds) → Promise</code></pre>	https://api.jquery.com/jQuery.when/
.keydown()	A			jQuery methods							Bind an event handler to the "keydown" JavaScript event, or trigger that event on an element.<pre><code>.keydown(handler, eventObject) → jQuery\n.keydown(eventData, handler, eventObject) → jQuery\n.keydown() → jQuery</code></pre>	https://api.jquery.com/keydown/
.keypress()	A			jQuery methods							Bind an event handler to the "keypress" JavaScript event, or trigger that event on an element.<pre><code>.keypress(handler, eventObject) → jQuery\n.keypress(eventData, handler, eventObject) → jQuery\n.keypress() → jQuery</code></pre>	https://api.jquery.com/keypress/
.keyup()	A			jQuery methods							Bind an event handler to the "keyup" JavaScript event, or trigger that event on an element.<pre><code>.keyup(handler, eventObject) → jQuery\n.keyup(eventData, handler, eventObject) → jQuery\n.keyup() → jQuery</code></pre>	https://api.jquery.com/keyup/
:lang() Selector	A			jQuery selectors							Selects all elements of the specified language.<pre><code>:lang(language) →  Selector</code></pre>	https://api.jquery.com/lang-selector/
.last()	A			jQuery methods							Reduce the set of matched elements to the final one in the set.<pre><code>.last() → jQuery</code></pre>	https://api.jquery.com/last/
:last-child Selector	A			jQuery selectors							Selects all elements that are the last child of their parent.<pre><code>:last-child Selector</code></pre>	https://api.jquery.com/last-child-selector/
:last-of-type Selector	A			jQuery selectors							Selects all elements that are the last among siblings of the same element name.<pre><code>:last-of-type Selector</code></pre>	https://api.jquery.com/last-of-type-selector/
:last Selector	A			jQuery selectors							Selects the last matched element.<pre><code>:last Selector</code></pre>	https://api.jquery.com/last-selector/
.length	A			jQuery properties							The number of elements in the jQuery object.<pre><code>.length</code></pre>	https://api.jquery.com/length/
.live()	A			jQuery methods							Attach an event handler for all elements which match the current selector, now and in the future.<pre><code>.live(events, handler, eventObject) → jQuery\n.live(events, data, handler, eventObject) → jQuery\n.live(events) → jQuery</code></pre>	https://api.jquery.com/live/
.load()	A			jQuery methods							Bind an event handler to the "load" JavaScript event.<pre><code>.load(handler, eventObject) → jQuery\n.load(eventData, handler, eventObject) → jQuery</code></pre>	https://api.jquery.com/load-event/
.load()	A			jQuery methods							Load data from the server and place the returned HTML into the matched element.<pre><code>.load(url, data, complete, responseText, textStatus, jqXHR) → jQuery</code></pre>	https://api.jquery.com/load/
:lt() Selector	A			jQuery selectors							Select all elements at an index less than index within the matched set.<pre><code>:lt(index) →  Selector\n:lt(indexFromEnd) →  Selector</code></pre>	https://api.jquery.com/lt-selector/
.map()	A			jQuery methods							Pass each element in the current matched set through a function, producing a new jQuery object containing the return values.<pre><code>.map(callback, index, domElement) → jQuery</code></pre>	https://api.jquery.com/map/
.mousedown()	A			jQuery methods							Bind an event handler to the "mousedown" JavaScript event, or trigger that event on an element.<pre><code>.mousedown(handler, eventObject) → jQuery\n.mousedown(eventData, handler, eventObject) → jQuery\n.mousedown() → jQuery</code></pre>	https://api.jquery.com/mousedown/
.mouseenter()	A			jQuery methods							Bind an event handler to be fired when the mouse enters an element, or trigger that handler on an element.<pre><code>.mouseenter(handler, eventObject) → jQuery\n.mouseenter(eventData, handler, eventObject) → jQuery\n.mouseenter() → jQuery</code></pre>	https://api.jquery.com/mouseenter/
.mouseleave()	A			jQuery methods							Bind an event handler to be fired when the mouse leaves an element, or trigger that handler on an element.<pre><code>.mouseleave(handler, eventObject) → jQuery\n.mouseleave(eventData, handler, eventObject) → jQuery\n.mouseleave() → jQuery</code></pre>	https://api.jquery.com/mouseleave/
.mousemove()	A			jQuery methods							Bind an event handler to the "mousemove" JavaScript event, or trigger that event on an element.<pre><code>.mousemove(handler, eventObject) → jQuery\n.mousemove(eventData, handler, eventObject) → jQuery\n.mousemove() → jQuery</code></pre>	https://api.jquery.com/mousemove/
.mouseout()	A			jQuery methods							Bind an event handler to the "mouseout" JavaScript event, or trigger that event on an element.<pre><code>.mouseout(handler, eventObject) → jQuery\n.mouseout(eventData, handler, eventObject) → jQuery\n.mouseout() → jQuery</code></pre>	https://api.jquery.com/mouseout/
.mouseover()	A			jQuery methods							Bind an event handler to the "mouseover" JavaScript event, or trigger that event on an element.<pre><code>.mouseover(handler, eventObject) → jQuery\n.mouseover(eventData, handler, eventObject) → jQuery\n.mouseover() → jQuery</code></pre>	https://api.jquery.com/mouseover/
.mouseup()	A			jQuery methods							Bind an event handler to the "mouseup" JavaScript event, or trigger that event on an element.<pre><code>.mouseup(handler, eventObject) → jQuery\n.mouseup(eventData, handler, eventObject) → jQuery\n.mouseup() → jQuery</code></pre>	https://api.jquery.com/mouseup/
Multiple Attribute Selector [name="value"][name2="value2"]	A			jQuery selectors							Matches elements that match all of the specified attribute filters.<pre><code>Multiple Attribute Selector [name="value"][name2="value2"]</code></pre>	https://api.jquery.com/multiple-attribute-selector/
Multiple Selector ("selector1, selector2, selectorN")	A			jQuery selectors							Selects the combined results of all the specified selectors.<pre><code>Multiple Selector ("selector1, selector2, selectorN")</code></pre>	https://api.jquery.com/multiple-selector/
Next Adjacent Selector ("prev + next")	A			jQuery selectors							Selects all next elements matching "next" that are immediately preceded by a sibling "prev".<pre><code>Next Adjacent Selector ("prev + next")</code></pre>	https://api.jquery.com/next-adjacent-selector/
.next()	A			jQuery methods							Get the immediately following sibling of each element in the set of matched elements. If a selector is provided, it retrieves the next sibling only if it matches that selector.<pre><code>.next(selector) → jQuery</code></pre>	https://api.jquery.com/next/
.nextAll()	A			jQuery methods							Get all following siblings of each element in the set of matched elements, optionally filtered by a selector.<pre><code>.nextAll(selector) → jQuery</code></pre>	https://api.jquery.com/nextAll/
Next Siblings Selector ("prev ~ siblings")	A			jQuery selectors							Selects all sibling elements that follow after the "prev" element, have the same parent, and match the filtering "siblings" selector.<pre><code>Next Siblings Selector ("prev ~ siblings")</code></pre>	https://api.jquery.com/next-siblings-selector/
.nextUntil()	A			jQuery methods							Get all following siblings of each element up to but not including the element matched by the selector, DOM node, or jQuery object passed.<pre><code>.nextUntil(selector, filter) → jQuery\n.nextUntil(element, filter) → jQuery</code></pre>	https://api.jquery.com/nextUntil/
.not()	A			jQuery methods							Remove elements from the set of matched elements.<pre><code>.not(selector) → jQuery\n.not(function, index, element) → jQuery\n.not(selection) → jQuery</code></pre>	https://api.jquery.com/not/
:not() Selector	A			jQuery selectors							Selects all elements that do not match the given selector.<pre><code>:not(selector) →  Selector</code></pre>	https://api.jquery.com/not-selector/
:nth-child() Selector	A			jQuery selectors							Selects all elements that are the nth-child of their parent.<pre><code>:nth-child(index) →  Selector</code></pre>	https://api.jquery.com/nth-child-selector/
:nth-last-child() Selector	A			jQuery selectors							Selects all elements that are the nth-child of their parent, counting from the last element to the first.<pre><code>:nth-last-child(index) →  Selector</code></pre>	https://api.jquery.com/nth-last-child-selector/
:nth-last-of-type() Selector	A			jQuery selectors							Selects all the elements that are the nth-child of their parent in relation to siblings with the same element name, counting from the last element to the first.<pre><code>:nth-last-of-type(index) →  Selector</code></pre>	https://api.jquery.com/nth-last-of-type-selector/
:nth-of-type() Selector	A			jQuery selectors							Selects all elements that are the nth child of their parent in relation to siblings with the same element name.<pre><code>:nth-of-type(index) →  Selector</code></pre>	https://api.jquery.com/nth-of-type-selector/
:odd Selector	A			jQuery selectors							Selects odd elements, zero-indexed.  See also even.<pre><code>:odd Selector</code></pre>	https://api.jquery.com/odd-selector/
.off()	A			jQuery methods							Remove an event handler.<pre><code>.off(events, selector, handler, eventObject) → jQuery\n.off(events, selector) → jQuery\n.off(event) → jQuery\n.off() → jQuery</code></pre>	https://api.jquery.com/off/
.offset()	A			jQuery methods							Get the current coordinates of the first element in the set of matched elements, relative to the document.Set the current coordinates of every element in the set of matched elements, relative to the document.<pre><code>.offset() → Object\n.offset(coordinates) → Object\n.offset(function, index, coords) → Object</code></pre>	https://api.jquery.com/offset/
.offsetParent()	A			jQuery methods							Get the closest ancestor element that is positioned.<pre><code>.offsetParent() → jQuery</code></pre>	https://api.jquery.com/offsetParent/
.on()	A			jQuery methods							Attach an event handler function for one or more events to the selected elements.<pre><code>.on(events, selector, data, handler, eventObject, extraParameter) → jQuery\n.on(events, selector, data) → jQuery</code></pre>	https://api.jquery.com/on/
.one()	A			jQuery methods							Attach a handler to an event for the elements. The handler is executed at most once per element per event type.<pre><code>.one(events, data, handler, eventObject) → jQuery\n.one(events, selector, data, handler, eventObject) → jQuery\n.one(events, selector, data) → jQuery</code></pre>	https://api.jquery.com/one/
:only-child Selector	A			jQuery selectors							Selects all elements that are the only child of their parent.<pre><code>:only-child Selector</code></pre>	https://api.jquery.com/only-child-selector/
:only-of-type Selector	A			jQuery selectors							Selects all elements that have no siblings with the same element name.<pre><code>:only-of-type Selector</code></pre>	https://api.jquery.com/only-of-type-selector/
.outerHeight()	A			jQuery methods							Get the current computed height for the first element in the set of matched elements, including padding, border, and optionally margin. Returns a number (without "px") representation of the value or null if called on an empty set of elements.Set the CSS outer Height of each element in the set of matched elements.<pre><code>.outerHeight(includeMargin) → Number\n.outerHeight(value) → Number\n.outerHeight(function(index, height)) → Number</code></pre>	https://api.jquery.com/outerHeight/
.outerWidth()	A			jQuery methods							Get the current computed width for the first element in the set of matched elements, including padding and border.Set the CSS outer width of each element in the set of matched elements.<pre><code>.outerWidth(includeMargin) → Number\n.outerWidth(value) → Number\n.outerWidth(function(index, width)) → Number</code></pre>	https://api.jquery.com/outerWidth/
.parent()	A			jQuery methods							Get the parent of each element in the current set of matched elements, optionally filtered by a selector.<pre><code>.parent(selector) → jQuery</code></pre>	https://api.jquery.com/parent/
.parents()	A			jQuery methods							Get the ancestors of each element in the current set of matched elements, optionally filtered by a selector.<pre><code>.parents(selector) → jQuery</code></pre>	https://api.jquery.com/parents/
:parent Selector	A			jQuery selectors							Select all elements that have at least one child node (either an element or text).<pre><code>:parent Selector</code></pre>	https://api.jquery.com/parent-selector/
.parentsUntil()	A			jQuery methods							Get the ancestors of each element in the current set of matched elements, up to but not including the element matched by the selector, DOM node, or jQuery object.<pre><code>.parentsUntil(selector, filter) → jQuery\n.parentsUntil(element, filter) → jQuery</code></pre>	https://api.jquery.com/parentsUntil/
:password Selector	A			jQuery selectors							Selects all elements of type password.<pre><code>:password Selector</code></pre>	https://api.jquery.com/password-selector/
.position()	A			jQuery methods							Get the current coordinates of the first element in the set of matched elements, relative to the offset parent.<pre><code>.position() → Object</code></pre>	https://api.jquery.com/position/
.prepend()	A			jQuery methods							Insert content, specified by the parameter, to the beginning of each element in the set of matched elements.<pre><code>.prepend(content, content) → jQuery\n.prepend(function, elementOfArray, html) → jQuery</code></pre>	https://api.jquery.com/prepend/
.prependTo()	A			jQuery methods							Insert every element in the set of matched elements to the beginning of the target.<pre><code>.prependTo(target) → jQuery</code></pre>	https://api.jquery.com/prependTo/
.prev()	A			jQuery methods							Get the immediately preceding sibling of each element in the set of matched elements. If a selector is provided, it retrieves the previous sibling only if it matches that selector.<pre><code>.prev(selector) → jQuery</code></pre>	https://api.jquery.com/prev/
.prevAll()	A			jQuery methods							Get all preceding siblings of each element in the set of matched elements, optionally filtered by a selector.<pre><code>.prevAll(selector) → jQuery</code></pre>	https://api.jquery.com/prevAll/
.prevUntil()	A			jQuery methods							Get all preceding siblings of each element up to but not including the element matched by the selector, DOM node, or jQuery object.<pre><code>.prevUntil(selector, filter) → jQuery\n.prevUntil(element, filter) → jQuery</code></pre>	https://api.jquery.com/prevUntil/
.promise()	A			jQuery methods							 Return a Promise object to observe when all actions of a certain type bound to the collection, queued or not, have finished. <pre><code>.promise(type, target) → Promise</code></pre>	https://api.jquery.com/promise/
.prop()	A			jQuery methods							Get the value of a property for the first element in the set of matched elements.Set one or more properties for the set of matched elements.<pre><code>.prop(propertyName) → \n.prop(propertyName, value) → \n.prop(properties) → \n.prop(propertyName, function, index, oldPropertyValue) → </code></pre>	https://api.jquery.com/prop/
.pushStack()	A			jQuery methods							Add a collection of DOM elements onto the jQuery stack.<pre><code>.pushStack(elements) → jQuery\n.pushStack(elements, name, arguments) → jQuery</code></pre>	https://api.jquery.com/pushStack/
.queue()	A			jQuery methods							Show the queue of functions to be executed on the matched elements.Manipulate the queue of functions to be executed, once for each matched element.<pre><code>.queue(queueName) → Array\n.queue(queueName, newQueue) → Array\n.queue(queueName, callback, next) → Array</code></pre>	https://api.jquery.com/queue/
:radio Selector	A			jQuery selectors							Selects all  elements of type radio.<pre><code>:radio Selector</code></pre>	https://api.jquery.com/radio-selector/
.ready()	A			jQuery methods							Specify a function to execute when the DOM is fully loaded.<pre><code>.ready(handler) → jQuery</code></pre>	https://api.jquery.com/ready/
.remove()	A			jQuery methods							Remove the set of matched elements from the DOM.<pre><code>.remove(selector) → jQuery</code></pre>	https://api.jquery.com/remove/
.removeAttr()	A			jQuery methods							Remove an attribute from each element in the set of matched elements.<pre><code>.removeAttr(attributeName) → jQuery</code></pre>	https://api.jquery.com/removeAttr/
.removeClass()	A			jQuery methods							Remove a single class, multiple classes, or all classes from each element in the set of matched elements.<pre><code>.removeClass(className) → jQuery\n.removeClass(function, index, className) → jQuery</code></pre>	https://api.jquery.com/removeClass/
.removeData()	A			jQuery methods							Remove a previously-stored piece of data.<pre><code>.removeData(name) → jQuery\n.removeData(list) → jQuery</code></pre>	https://api.jquery.com/removeData/
.removeProp()	A			jQuery methods							Remove a property for the set of matched elements.<pre><code>.removeProp(propertyName) → jQuery</code></pre>	https://api.jquery.com/removeProp/
.replaceAll()	A			jQuery methods							Replace each target element with the set of matched elements.<pre><code>.replaceAll(target) → jQuery</code></pre>	https://api.jquery.com/replaceAll/
.replaceWith()	A			jQuery methods							Replace each element in the set of matched elements with the provided new content and return the set of elements that was removed.<pre><code>.replaceWith(newContent) → jQuery\n.replaceWith(function) → jQuery</code></pre>	https://api.jquery.com/replaceWith/
:reset Selector	A			jQuery selectors							Selects all elements of type reset.<pre><code>:reset Selector</code></pre>	https://api.jquery.com/reset-selector/
.resize()	A			jQuery methods							Bind an event handler to the "resize" JavaScript event, or trigger that event on an element.<pre><code>.resize(handler, eventObject) → jQuery\n.resize(eventData, handler, eventObject) → jQuery\n.resize() → jQuery</code></pre>	https://api.jquery.com/resize/
:root Selector	A			jQuery selectors							Selects the element that is the root of the document.<pre><code>:root Selector</code></pre>	https://api.jquery.com/root-selector/
.scroll()	A			jQuery methods							Bind an event handler to the "scroll" JavaScript event, or trigger that event on an element.<pre><code>.scroll(handler, eventObject) → jQuery\n.scroll(eventData, handler, eventObject) → jQuery\n.scroll() → jQuery</code></pre>	https://api.jquery.com/scroll/
.scrollLeft()	A			jQuery methods							Get the current horizontal position of the scroll bar for the first element in the set of matched elements.Set the current horizontal position of the scroll bar for each of the set of matched elements.<pre><code>.scrollLeft() → Integer\n.scrollLeft(value) → Integer</code></pre>	https://api.jquery.com/scrollLeft/
.scrollTop()	A			jQuery methods							Get the current vertical position of the scroll bar for the first element in the set of matched elements or set the vertical position of the scroll bar for every matched element.Set the current vertical position of the scroll bar for each of the set of matched elements.<pre><code>.scrollTop() → Number\n.scrollTop(value) → Number</code></pre>	https://api.jquery.com/scrollTop/
.select()	A			jQuery methods							Bind an event handler to the "select" JavaScript event, or trigger that event on an element.<pre><code>.select(handler, eventObject) → jQuery\n.select(eventData, handler, eventObject) → jQuery\n.select() → jQuery</code></pre>	https://api.jquery.com/select/
:selected Selector	A			jQuery selectors							Selects all elements that are selected.<pre><code>:selected Selector</code></pre>	https://api.jquery.com/selected-selector/
.selector	A			jQuery properties							A selector representing selector passed to jQuery(), if any, when creating the original set.<pre><code>.selector</code></pre>	https://api.jquery.com/selector/
.serialize()	A			jQuery methods							Encode a set of form elements as a string for submission.<pre><code>.serialize() → String</code></pre>	https://api.jquery.com/serialize/
.serializeArray()	A			jQuery methods							Encode a set of form elements as an array of names and values.<pre><code>.serializeArray() → Array</code></pre>	https://api.jquery.com/serializeArray/
.show()	A			jQuery methods							Display the matched elements.<pre><code>.show() → jQuery\n.show() → jQuery\n.show() → jQuery\n.show(duration) → jQuery</code></pre>	https://api.jquery.com/show/
.siblings()	A			jQuery methods							Get the siblings of each element in the set of matched elements, optionally filtered by a selector.<pre><code>.siblings(selector) → jQuery</code></pre>	https://api.jquery.com/siblings/
.size()	A			jQuery methods							Return the number of elements in the jQuery object.<pre><code>.size() → Integer</code></pre>	https://api.jquery.com/size/
.slice()	A			jQuery methods							Reduce the set of matched elements to a subset specified by a range of indices.<pre><code>.slice(start, end) → jQuery</code></pre>	https://api.jquery.com/slice/
.slideDown()	A			jQuery methods							Display the matched elements with a sliding motion.<pre><code>.slideDown() → jQuery\n.slideDown() → jQuery\n.slideDown() → jQuery</code></pre>	https://api.jquery.com/slideDown/
.slideToggle()	A			jQuery methods							Display or hide the matched elements with a sliding motion.<pre><code>.slideToggle() → jQuery\n.slideToggle() → jQuery\n.slideToggle() → jQuery</code></pre>	https://api.jquery.com/slideToggle/
.slideUp()	A			jQuery methods							Hide the matched elements with a sliding motion.<pre><code>.slideUp() → jQuery\n.slideUp() → jQuery\n.slideUp() → jQuery</code></pre>	https://api.jquery.com/slideUp/
.stop()	A			jQuery methods							Stop the currently-running animation on the matched elements.<pre><code>.stop(clearQueue, jumpToEnd) → jQuery\n.stop(queue, clearQueue, jumpToEnd) → jQuery</code></pre>	https://api.jquery.com/stop/
.submit()	A			jQuery methods							Bind an event handler to the "submit" JavaScript event, or trigger that event on an element.<pre><code>.submit(handler, eventObject) → jQuery\n.submit(eventData, handler, eventObject) → jQuery\n.submit() → jQuery</code></pre>	https://api.jquery.com/submit/
:submit Selector	A			jQuery selectors							Selects all elements of type submit.<pre><code>:submit Selector</code></pre>	https://api.jquery.com/submit-selector/
:target Selector	A			jQuery selectors							Selects the target element indicated by the fragment identifier of the document's URI.<pre><code>:target Selector</code></pre>	https://api.jquery.com/target-selector/
.text()	A			jQuery methods							Get the combined text contents of each element in the set of matched elements, including their descendants.Set the content of each element in the set of matched elements to the specified text.<pre><code>.text() → String\n.text(text) → String\n.text(function, index, text) → String</code></pre>	https://api.jquery.com/text/
:text Selector	A			jQuery selectors							Selects all input elements of type text.<pre><code>:text Selector</code></pre>	https://api.jquery.com/text-selector/
.toArray()	A			jQuery methods							Retrieve all the elements contained in the jQuery set, as an array.<pre><code>.toArray() → Array</code></pre>	https://api.jquery.com/toArray/
.toggle()	A			jQuery methods							Bind two or more handlers to the matched elements, to be executed on alternate clicks.<pre><code>.toggle(handler, eventObject, handler, eventObject, handler, eventObject) → jQuery</code></pre>	https://api.jquery.com/toggle-event/
.toggle()	A			jQuery methods							Display or hide the matched elements.<pre><code>.toggle() → jQuery\n.toggle() → jQuery\n.toggle(duration) → jQuery\n.toggle(display) → jQuery</code></pre>	https://api.jquery.com/toggle/
.toggleClass()	A			jQuery methods							Add or remove one or more classes from each element in the set of matched elements, depending on either the class's presence or the value of the state argument.<pre><code>.toggleClass(className) → jQuery\n.toggleClass(className, state) → jQuery\n.toggleClass(function, index, className, state, state) → jQuery\n.toggleClass(state) → jQuery</code></pre>	https://api.jquery.com/toggleClass/
.trigger()	A			jQuery methods							Execute all handlers and behaviors attached to the matched elements for the given event type.<pre><code>.trigger(eventType, extraParameters) → jQuery\n.trigger(event, extraParameters) → jQuery</code></pre>	https://api.jquery.com/trigger/
.triggerHandler()	A			jQuery methods							Execute all handlers attached to an element for an event.<pre><code>.triggerHandler(eventType, extraParameters) → Object\n.triggerHandler(event, extraParameters) → Object</code></pre>	https://api.jquery.com/triggerHandler/
.unbind()	A			jQuery methods							Remove a previously-attached event handler from the elements.<pre><code>.unbind(eventType, handler, eventObject) → jQuery\n.unbind(eventType, false) → jQuery\n.unbind(event) → jQuery\n.unbind() → jQuery</code></pre>	https://api.jquery.com/unbind/
.undelegate()	A			jQuery methods							Remove a handler from the event for all elements which match the current selector, based upon a specific set of root elements.<pre><code>.undelegate() → jQuery\n.undelegate(selector, eventType) → jQuery\n.undelegate(selector, eventType, handler, eventObject) → jQuery\n.undelegate(selector, events) → jQuery\n.undelegate(namespace) → jQuery</code></pre>	https://api.jquery.com/undelegate/
.unload()	A			jQuery methods							Bind an event handler to the "unload" JavaScript event.<pre><code>.unload(handler, eventObject) → jQuery\n.unload(eventData, handler, eventObject) → jQuery</code></pre>	https://api.jquery.com/unload/
.unwrap()	A			jQuery methods							Remove the parents of the set of matched elements from the DOM, leaving the matched elements in their place.<pre><code>.unwrap() → jQuery\n.unwrap(selector) → jQuery</code></pre>	https://api.jquery.com/unwrap/
.val()	A			jQuery methods							Get the current value of the first element in the set of matched elements.Set the value of each element in the set of matched elements.<pre><code>.val() → \n.val(value) → \n.val(function, index, value) → </code></pre>	https://api.jquery.com/val/
:visible Selector	A			jQuery selectors							Selects all elements that are visible.<pre><code>:visible Selector</code></pre>	https://api.jquery.com/visible-selector/
.width()	A			jQuery methods							Get the current computed width for the first element in the set of matched elements.Set the CSS width of each element in the set of matched elements.<pre><code>.width() → Number\n.width(value) → Number\n.width(function, index, value) → Number</code></pre>	https://api.jquery.com/width/
.wrap()	A			jQuery methods							Wrap an HTML structure around each element in the set of matched elements.<pre><code>.wrap(wrappingElement) → jQuery\n.wrap(function, index) → jQuery</code></pre>	https://api.jquery.com/wrap/
.wrapAll()	A			jQuery methods							Wrap an HTML structure around all elements in the set of matched elements.<pre><code>.wrapAll(wrappingElement) → jQuery\n.wrapAll(function) → jQuery</code></pre>	https://api.jquery.com/wrapAll/
.wrapInner()	A			jQuery methods							Wrap an HTML structure around the content of each element in the set of matched elements.<pre><code>.wrapInner(wrappingElement) → jQuery\n.wrapInner(function, index) → jQuery</code></pre>	https://api.jquery.com/wrapInner/
