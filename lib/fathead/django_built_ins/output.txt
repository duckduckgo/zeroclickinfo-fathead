autoescape	A							https://www.djangoproject.com/			<pre><code>{% autoescape on %}
    {{ body }}
{% endautoescape %}
</code></pre><br>Controls the current auto-escaping behavior. This tag takes either on or off as an argument and that determines whether auto-escaping is in effect inside the block. The block is closed with an endautoescape ending tag.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#autoescape
block	A							https://www.djangoproject.com/			<br>Defines a block that can be overridden by child templates. See Template inheritance for more information.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#block
comment	A							https://www.djangoproject.com/			<pre><code><p>Rendered text with {{ pub_date|date:"c" }}</p>
{% comment "Optional note" %}
    <p>Commented out text with {{ create_date|date:"c" }}</p>
{% endcomment %}
</code></pre><br>Ignores everything between {% comment %} and {% endcomment %}. An optional note may be inserted in the first tag. For example, this is useful when commenting out code for documenting why the code was disabled.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#comment
csrf_token	A							https://www.djangoproject.com/			<br>This tag is used for CSRF protection, as described in the documentation for Cross Site Request Forgeries.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#csrf-token
cycle	A							https://www.djangoproject.com/			<pre><code>{% for o in some_list %}
    <tr class="{% cycle 'row1' 'row2' %}">
        ...
    </tr>
{% endfor %}
</code></pre><br>Produces one of its arguments each time this tag is encountered. The first argument is produced on the first encounter, the second argument on the second encounter, and so forth. Once all arguments are exhausted, the tag cycles to the first argument and produces it again.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#cycle
debug	A							https://www.djangoproject.com/			<br>Outputs a whole load of debugging information, including the current context and imported modules.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#debug
extends	A							https://www.djangoproject.com/			<br>Signals that this template extends a parent template.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#extends
filter	A							https://www.djangoproject.com/			<pre><code>{% filter force_escape|lower %}
    This text will be HTML-escaped, and will appear in all lowercase.
{% endfilter %}
</code></pre><br>Filters the contents of the block through one or more filters. Multiple filters can be specified with pipes and filters can have arguments, just as in variable syntax.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#filter
firstof	A							https://www.djangoproject.com/			<pre><code>{% firstof var1 var2 var3 %}
</code></pre><br>Outputs the first argument variable that is not False. Outputs nothing if all the passed variables are False.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#firstof
for	A							https://www.djangoproject.com/			<pre><code><ul>
{% for athlete in athlete_list %}
    <li>{{ athlete.name }}</li>
{% endfor %}
</ul>
</code></pre><br>Loops over each item in an array, making the item available in a context variable. For example, to display a list of athletes provided in athlete_list:	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#for
for	A							https://www.djangoproject.com/			<pre><code><ul>
{% for athlete in athlete_list %}
    <li>{{ athlete.name }}</li>
{% empty %}
    <li>Sorry, no athletes in this list.</li>
{% endfor %}
</ul>
</code></pre><br>The for tag can take an optional {% empty %} clause whose text is displayed if the given array is empty or could not be found:	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#for-empty
if	A							https://www.djangoproject.com/			<pre><code>{% if athlete_list %}
    Number of athletes: {{ athlete_list|length }}
{% elif athlete_in_locker_room_list %}
    Athletes should be out of the locker room soon!
{% else %}
    No athletes.
{% endif %}
</code></pre><br>The {% if %} tag evaluates a variable, and if that variable is “true” (i.e. exists, is not empty, and is not a false boolean value) the contents of the block are output:	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#if
ifequal	A							https://www.djangoproject.com/			<br>{% ifequal a b %} ... {% endifequal %} is an obsolete way to write {% if a == b %} ... {% endif %}. Likewise, {% ifnotequal a b %} ... {% endifnotequal %} is superseded by {% if a != b %} ... {% endif %}. The ifequal and ifnotequal tags will be deprecated in a future release.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#ifequal-and-ifnotequal
ifchanged	A							https://www.djangoproject.com/			<pre><code><h1>Archive for {{ year }}</h1>

{% for date in days %}
    {% ifchanged %}<h3>{{ date|date:"F" }}</h3>{% endifchanged %}
    <a href="{{ date|date:"M/d"|lower }}/">{{ date|date:"j" }}</a>
{% endfor %}
</code></pre><br>Check if a value has changed from the last iteration of a loop.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#ifchanged
include	A							https://www.djangoproject.com/			<pre><code>{% include "foo/bar.html" %}
</code></pre><br>Loads a template and renders it with the current context. This is a way of “including” other templates within a template.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#include
load	A							https://www.djangoproject.com/			<pre><code>{% load somelibrary package.otherlibrary %}
</code></pre><br>Loads a custom template tag set.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#load
lorem	A							https://www.djangoproject.com/			<pre><code>{% lorem [count] [method] [random] %}
</code></pre><br>The tag was previously located in django.contrib.webdesign.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#lorem
now	A							https://www.djangoproject.com/			<pre><code>It is {% now "jS F Y H:i" %}
</code></pre><br>Displays the current date and/or time, using a format according to the given string. Such string can contain format specifiers characters as described in the date filter section.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#now
regroup	A							https://www.djangoproject.com/			<pre><code>cities = [
    {'name': 'Mumbai', 'population': '19,000,000', 'country': 'India'},
    {'name': 'Calcutta', 'population': '15,000,000', 'country': 'India'},
    {'name': 'New York', 'population': '20,000,000', 'country': 'USA'},
    {'name': 'Chicago', 'population': '7,000,000', 'country': 'USA'},
    {'name': 'Tokyo', 'population': '33,000,000', 'country': 'Japan'},
]
</code></pre><br>Regroups a list of alike objects by a common attribute.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#regroup
spaceless	A							https://www.djangoproject.com/			<pre><code>{% spaceless %}
    <p>
        <a href="foo/">Foo</a>
    </p>
{% endspaceless %}
</code></pre><br>Removes whitespace between HTML tags. This includes tab characters and newlines.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#spaceless
templatetag	A							https://www.djangoproject.com/			<pre><code>{% templatetag openblock %} url 'entry_list' {% templatetag closeblock %}
</code></pre><br>Outputs one of the syntax characters used to compose template tags.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#templatetag
url	A							https://www.djangoproject.com/			<pre><code>{% url 'some-url-name' v1 v2 %}
</code></pre><br>Returns an absolute path reference (a URL without the domain name) matching a given view and optional parameters. Any special characters in the resulting path will be encoded using iri_to_uri().	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#url
verbatim	A							https://www.djangoproject.com/			<pre><code>{% verbatim %}
    {{if dying}}Still alive.{{/if}}
{% endverbatim %}
</code></pre><br>Stops the template engine from rendering the contents of this block tag.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#verbatim
widthratio	A							https://www.djangoproject.com/			<pre><code><img src="bar.png" alt="Bar"
     height="10" width="{% widthratio this_value max_value max_width %}" />
</code></pre><br>For creating bar charts and such, this tag calculates the ratio of a given value to a maximum value, and then applies that ratio to a constant.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#widthratio
with	A							https://www.djangoproject.com/			<pre><code>{% with total=business.employees.count %}
    {{ total }} employee{{ total|pluralize }}
{% endwith %}
</code></pre><br>Caches a complex variable under a simpler name. This is useful when accessing an “expensive” method (e.g., one that hits the database) multiple times.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#with
add	A							https://www.djangoproject.com/			<pre><code>{{ value|add:"2" }}
</code></pre><br>Adds the argument to the value.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#add
addslashes	A							https://www.djangoproject.com/			<pre><code>{{ value|addslashes }}
</code></pre><br>Adds slashes before quotes. Useful for escaping strings in CSV, for example.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#addslashes
capfirst	A							https://www.djangoproject.com/			<pre><code>{{ value|capfirst }}
</code></pre><br>Capitalizes the first character of the value. If the first character is not a letter, this filter has no effect.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#capfirst
center	A							https://www.djangoproject.com/			<pre><code>"{{ value|center:"15" }}"
</code></pre><br>Centers the value in a field of a given width.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#center
cut	A							https://www.djangoproject.com/			<pre><code>{{ value|cut:" " }}
</code></pre><br>Removes all values of arg from the given string.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#cut
date	A							https://www.djangoproject.com/			<pre><code>{{ value|date:"D d M Y" }}
</code></pre><br>Formats a date according to the given format.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#date
default	A							https://www.djangoproject.com/			<pre><code>{{ value|default:"nothing" }}
</code></pre><br>If value evaluates to False, uses the given default. Otherwise, uses the value.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#default
default_if_none	A							https://www.djangoproject.com/			<pre><code>{{ value|default_if_none:"nothing" }}
</code></pre><br>If (and only if) value is None, uses the given default. Otherwise, uses the value.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#default-if-none
dictsort	A							https://www.djangoproject.com/			<pre><code>{{ value|dictsort:"name" }}
</code></pre><br>Takes a list of dictionaries and returns that list sorted by the key given in the argument.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#dictsort
dictsortreversed	A							https://www.djangoproject.com/			<br>Takes a list of dictionaries and returns that list sorted in reverse order by the key given in the argument. This works exactly the same as the above filter, but the returned value will be in reverse order.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#dictsortreversed
divisibleby	A							https://www.djangoproject.com/			<pre><code>{{ value|divisibleby:"3" }}
</code></pre><br>Returns True if the value is divisible by the argument.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#divisibleby
escape	A							https://www.djangoproject.com/			<pre><code>{% autoescape off %}
    {{ title|escape }}
{% endautoescape %}
</code></pre><br>Escapes a string’s HTML. Specifically, it makes these replacements:	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#escape
escapejs	A							https://www.djangoproject.com/			<pre><code>{{ value|escapejs }}
</code></pre><br>Escapes characters for use in JavaScript strings. This does not make the string safe for use in HTML, but does protect you from syntax errors when using templates to generate JavaScript/JSON.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#escapejs
filesizeformat	A							https://www.djangoproject.com/			<pre><code>{{ value|filesizeformat }}
</code></pre><br>Formats the value like a ‘human-readable’ file size (i.e. '13 KB', '4.1 MB', '102 bytes', etc.).	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#filesizeformat
first	A							https://www.djangoproject.com/			<pre><code>{{ value|first }}
</code></pre><br>Returns the first item in a list.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#first
floatformat	A							https://www.djangoproject.com/			<br>When used without an argument, rounds a floating-point number to one decimal place – but only if there’s a decimal part to be displayed. For example:	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#floatformat
force_escape	A							https://www.djangoproject.com/			<pre><code>{% autoescape off %}
    {{ body|linebreaks|force_escape }}
{% endautoescape %}
</code></pre><br>Applies HTML escaping to a string (see the escape filter for details). This filter is applied immediately and returns a new, escaped string. This is useful in the rare cases where you need multiple escaping or want to apply other filters to the escaped results. Normally, you want to use the escape filter.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#force-escape
get_digit	A							https://www.djangoproject.com/			<pre><code>{{ value|get_digit:"2" }}
</code></pre><br>Given a whole number, returns the requested digit, where 1 is the right-most digit, 2 is the second-right-most digit, etc. Returns the original value for invalid input (if input or argument is not an integer, or if argument is less than 1). Otherwise, output is always an integer.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#get-digit
iriencode	A							https://www.djangoproject.com/			<pre><code>{{ value|iriencode }}
</code></pre><br>Converts an IRI (Internationalized Resource Identifier) to a string that is suitable for including in a URL. This is necessary if you’re trying to use strings containing non-ASCII characters in a URL.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#iriencode
join	A							https://www.djangoproject.com/			<pre><code>{{ value|join:" // " }}
</code></pre><br>Joins a list with a string, like Python’s str.join(list)	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#join
last	A							https://www.djangoproject.com/			<pre><code>{{ value|last }}
</code></pre><br>Returns the last item in a list.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#last
length	A							https://www.djangoproject.com/			<pre><code>{{ value|length }}
</code></pre><br>Returns the length of the value. This works for both strings and lists.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#length
length_is	A							https://www.djangoproject.com/			<pre><code>{{ value|length_is:"4" }}
</code></pre><br>Returns True if the value’s length is the argument, or False otherwise.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#length-is
linebreaks	A							https://www.djangoproject.com/			<pre><code>{{ value|linebreaks }}
</code></pre><br>Replaces line breaks in plain text with appropriate HTML; a single newline becomes an HTML line break (<br />) and a new line followed by a blank line becomes a paragraph break (</p>).	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#linebreaks
linebreaksbr	A							https://www.djangoproject.com/			<pre><code>{{ value|linebreaksbr }}
</code></pre><br>Converts all newlines in a piece of plain text to HTML line breaks (<br />).	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#linebreaksbr
linenumbers	A							https://www.djangoproject.com/			<pre><code>{{ value|linenumbers }}
</code></pre><br>Displays text with line numbers.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#linenumbers
ljust	A							https://www.djangoproject.com/			<pre><code>"{{ value|ljust:"10" }}"
</code></pre><br>Left-aligns the value in a field of a given width.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#ljust
lower	A							https://www.djangoproject.com/			<pre><code>{{ value|lower }}
</code></pre><br>Converts a string into all lowercase.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#lower
make_list	A							https://www.djangoproject.com/			<pre><code>{{ value|make_list }}
</code></pre><br>Returns the value turned into a list. For a string, it’s a list of characters. For an integer, the argument is cast into an unicode string before creating a list.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#make-list
phone2numeric	A							https://www.djangoproject.com/			<pre><code>{{ value|phone2numeric }}
</code></pre><br>Converts a phone number (possibly containing letters) to its numerical equivalent.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#phone2numeric
pluralize	A							https://www.djangoproject.com/			<pre><code>You have {{ num_messages }} message{{ num_messages|pluralize }}.
</code></pre><br>Returns a plural suffix if the value is not 1. By default, this suffix is 's'.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#pluralize
pprint	A							https://www.djangoproject.com/			<br>A wrapper around pprint.pprint() – for debugging, really.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#pprint
random	A							https://www.djangoproject.com/			<pre><code>{{ value|random }}
</code></pre><br>Returns a random item from the given list.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#random
rjust	A							https://www.djangoproject.com/			<pre><code>"{{ value|rjust:"10" }}"
</code></pre><br>Right-aligns the value in a field of a given width.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#rjust
safe	A							https://www.djangoproject.com/			<pre><code>{{ var|safe|escape }}
</code></pre><br>Marks a string as not requiring further HTML escaping prior to output. When autoescaping is off, this filter has no effect.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#safe
safeseq	A							https://www.djangoproject.com/			<pre><code>{{ some_list|safeseq|join:", " }}
</code></pre><br>Applies the safe filter to each element of a sequence. Useful in conjunction with other filters that operate on sequences, such as join. For example:	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#safeseq
slice	A							https://www.djangoproject.com/			<pre><code>{{ some_list|slice:":2" }}
</code></pre><br>Returns a slice of the list.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#slice
slugify	A							https://www.djangoproject.com/			<pre><code>{{ value|slugify }}
</code></pre><br>Converts to ASCII. Converts spaces to hyphens. Removes characters that aren’t alphanumerics, underscores, or hyphens. Converts to lowercase. Also strips leading and trailing whitespace.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#slugify
stringformat	A							https://www.djangoproject.com/			<pre><code>{{ value|stringformat:"E" }}
</code></pre><br>Formats the variable according to the argument, a string formatting specifier. This specifier uses the printf-style String Formatting syntax, with the exception that the leading “%” is dropped.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#stringformat
striptags	A							https://www.djangoproject.com/			<pre><code>{{ value|striptags }}
</code></pre><br>Makes all possible efforts to strip all [X]HTML tags.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#striptags
time	A							https://www.djangoproject.com/			<pre><code>{{ value|time:"H:i" }}
</code></pre><br>Formats a time according to the given format.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#time
timesince	A							https://www.djangoproject.com/			<pre><code>{{ blog_date|timesince:comment_date }}
</code></pre><br>Formats a date as the time since that date (e.g., “4 days, 6 hours”).	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#timesince
timeuntil	A							https://www.djangoproject.com/			<pre><code>{{ conference_date|timeuntil:from_date }}
</code></pre><br>Similar to timesince, except that it measures the time from now until the given date or datetime. For example, if today is 1 June 2006 and conference_date is a date instance holding 29 June 2006, then {{ conference_date|timeuntil }} will return “4 weeks”.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#timeuntil
title	A							https://www.djangoproject.com/			<pre><code>{{ value|title }}
</code></pre><br>Converts a string into titlecase by making words start with an uppercase character and the remaining characters lowercase. This tag makes no effort to keep “trivial words” in lowercase.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#title
truncatechars	A							https://www.djangoproject.com/			<pre><code>{{ value|truncatechars:9 }}
</code></pre><br>Truncates a string if it is longer than the specified number of characters. Truncated strings will end with a translatable ellipsis sequence (”...”).	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#truncatechars
truncatechars_html	A							https://www.djangoproject.com/			<pre><code>{{ value|truncatechars_html:9 }}
</code></pre><br>Similar to truncatechars, except that it is aware of HTML tags. Any tags that are opened in the string and not closed before the truncation point are closed immediately after the truncation.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#truncatechars-html
truncatewords	A							https://www.djangoproject.com/			<pre><code>{{ value|truncatewords:2 }}
</code></pre><br>Truncates a string after a certain number of words.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#truncatewords
truncatewords_html	A							https://www.djangoproject.com/			<pre><code>{{ value|truncatewords_html:2 }}
</code></pre><br>Similar to truncatewords, except that it is aware of HTML tags. Any tags that are opened in the string and not closed before the truncation point, are closed immediately after the truncation.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#truncatewords-html
unordered_list	A							https://www.djangoproject.com/			<pre><code><li>States
<ul>
        <li>Kansas
        <ul>
                <li>Lawrence</li>
                <li>Topeka</li>
        </ul>
        </li>
        <li>Illinois</li>
</ul>
</li>
</code></pre><br>Recursively takes a self-nested list and returns an HTML unordered list – WITHOUT opening and closing <ul> tags.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#unordered-list
upper	A							https://www.djangoproject.com/			<pre><code>{{ value|upper }}
</code></pre><br>Converts a string into all uppercase.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#upper
urlencode	A							https://www.djangoproject.com/			<pre><code>{{ value|urlencode }}
</code></pre><br>Escapes a value for use in a URL.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#urlencode
urlize	A							https://www.djangoproject.com/			<pre><code>{{ value|urlize }}
</code></pre><br>Converts URLs and email addresses in text into clickable links.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#urlize
urlizetrunc	A							https://www.djangoproject.com/			<pre><code>{{ value|urlizetrunc:15 }}
</code></pre><br>Converts URLs and email addresses into clickable links just like urlize, but truncates URLs longer than the given character limit.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#urlizetrunc
wordcount	A							https://www.djangoproject.com/			<pre><code>{{ value|wordcount }}
</code></pre><br>Returns the number of words.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#wordcount
wordwrap	A							https://www.djangoproject.com/			<pre><code>{{ value|wordwrap:5 }}
</code></pre><br>Wraps words at specified line length.	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#wordwrap
yesno	A							https://www.djangoproject.com/			<pre><code>{{ value|yesno:"yeah,no,maybe" }}
</code></pre><br>Maps values for True, False, and (optionally) None, to the strings “yes”, “no”, “maybe”, or a custom mapping passed as a comma-separated list, and returns one of those strings according to the value:	https://docs.djangoproject.com/en/1.9/ref/templates/builtins/#yesno
