.zirz	A											The MIME type for the extension <code>.zirz</code> is <code>application/vnd.zul</code>	
.conf	A											The MIME type for the extension <code>.conf</code> is <code>text/plain</code>	
.obd	A											The MIME type for the extension <code>.obd</code> is <code>application/x-msbinder</code>	
.obj	A											The MIME type for the extension <code>.obj</code> is <code>application/octet-stream</code>	
.dtshd	A											The MIME type for the extension <code>.dtshd</code> is <code>audio/vnd.dts.hd</code>	
.silo	A											The MIME type for the extension <code>.silo</code> is <code>model/mesh</code>	
.pml	A											The MIME type for the extension <code>.pml</code> is <code>application/vnd.ctc-posml</code>	
.f4v	A											The MIME type for the extension <code>.f4v</code> is <code>video/x-f4v</code>	
.gam	A											The MIME type for the extension <code>.gam</code> is <code>chemical/x-gamess-input</code>	
.iso	A											The MIME type for the extension <code>.iso</code> is <code>application/x-iso9660-image</code>	
.hvs	A											The MIME type for the extension <code>.hvs</code> is <code>application/vnd.yamaha.hv-script</code>	
.ras	A											The MIME type for the extension <code>.ras</code> is <code>image/x-cmu-raster</code>	
.rar	A											The MIME type for the extension <code>.rar</code> is <code>application/x-rar-compressed</code>	
.embl	A											The MIME type for the extension <code>.embl</code> is <code>chemical/x-embl-dl-nucleotide</code>	
.mrc	A											The MIME type for the extension <code>.mrc</code> is <code>application/marc</code>	
.sdkd	A											The MIME type for the extension <code>.sdkd</code> is <code>application/vnd.solent.sdkm+xml</code>	
.bcpio	A											The MIME type for the extension <code>.bcpio</code> is <code>application/x-bcpio</code>	
.asf	A											The MIME type for the extension <code>.asf</code> is <code>video/x-ms-asf</code>	
.sdkm	A											The MIME type for the extension <code>.sdkm</code> is <code>application/vnd.solent.sdkm+xml</code>	
.ctx	A											The MIME type for the extension <code>.ctx</code> is <code>chemical/x-ctx</code>	
.list3820	A											The MIME type for the extension <code>.list3820</code> is <code>application/vnd.ibm.modcap</code>	
.ecelp7470	A											The MIME type for the extension <code>.ecelp7470</code> is <code>audio/vnd.nuera.ecelp7470</code>	
.ist	A											The MIME type for the extension <code>.ist</code> is <code>chemical/x-isostar</code>	
.fh5	A											The MIME type for the extension <code>.fh5</code> is <code>image/x-freehand</code>	
.bz	A											The MIME type for the extension <code>.bz</code> is <code>application/x-bzip</code>	
.onetoc2	A											The MIME type for the extension <code>.onetoc2</code> is <code>application/onenote</code>	
.onetmp	A											The MIME type for the extension <code>.onetmp</code> is <code>application/onenote</code>	
.bmp	A											The MIME type for the extension <code>.bmp</code> is <code>image/x-ms-bmp</code>	
.gen	A											The MIME type for the extension <code>.gen</code> is <code>chemical/x-genbank</code>	
.geo	A											The MIME type for the extension <code>.geo</code> is <code>application/vnd.dynageo</code>	
.air	A											The MIME type for the extension <code>.air</code> is <code>application/vnd.adobe.air-application-installer-package+zip</code>	
.bmi	A											The MIME type for the extension <code>.bmi</code> is <code>application/vnd.bmi</code>	
.jad	A											The MIME type for the extension <code>.jad</code> is <code>text/vnd.sun.j2me.app-descriptor</code>	
.xhvml	A											The MIME type for the extension <code>.xhvml</code> is <code>application/xv+xml</code>	
.dvi	A											The MIME type for the extension <code>.dvi</code> is <code>application/x-dvi</code>	
.gex	A											The MIME type for the extension <code>.gex</code> is <code>application/vnd.geometry-explorer</code>	
.gjf	A											The MIME type for the extension <code>.gjf</code> is <code>chemical/x-gaussian-input</code>	
.hpid	A											The MIME type for the extension <code>.hpid</code> is <code>application/vnd.hp-hpid</code>	
.ott	A											The MIME type for the extension <code>.ott</code> is <code>application/vnd.oasis.opendocument.text-template</code>	
.rss	A											The MIME type for the extension <code>.rss</code> is <code>application/rss+xml</code>	
.ots	A											The MIME type for the extension <code>.ots</code> is <code>application/vnd.oasis.opendocument.spreadsheet-template</code>	
.hlp	A											The MIME type for the extension <code>.hlp</code> is <code>application/winhlp</code>	
.cst	A											The MIME type for the extension <code>.cst</code> is <code>application/x-director</code>	
.p10	A											The MIME type for the extension <code>.p10</code> is <code>application/pkcs10</code>	
.csv	A											The MIME type for the extension <code>.csv</code> is <code>text/csv</code>	
.p12	A											The MIME type for the extension <code>.p12</code> is <code>application/x-pkcs12</code>	
.csp	A											The MIME type for the extension <code>.csp</code> is <code>application/vnd.commonspace</code>	
.sus	A											The MIME type for the extension <code>.sus</code> is <code>application/vnd.sus-calendar</code>	
.css	A											The MIME type for the extension <code>.css</code> is <code>text/css</code>	
.csm	A											The MIME type for the extension <code>.csm</code> is <code>chemical/x-csml</code>	
.otg	A											The MIME type for the extension <code>.otg</code> is <code>application/vnd.oasis.opendocument.graphics-template</code>	
.otf	A											The MIME type for the extension <code>.otf</code> is <code>application/x-font-otf</code>	
.csh	A											The MIME type for the extension <code>.csh</code> is <code>text/x-csh</code>	
.otc	A											The MIME type for the extension <code>.otc</code> is <code>application/vnd.oasis.opendocument.chart-template</code>	
.otm	A											The MIME type for the extension <code>.otm</code> is <code>application/vnd.oasis.opendocument.text-master</code>	
.csf	A											The MIME type for the extension <code>.csf</code> is <code>chemical/x-cache-csf</code>	
.mcif	A											The MIME type for the extension <code>.mcif</code> is <code>chemical/x-mmcif</code>	
.pdf	A											The MIME type for the extension <code>.pdf</code> is <code>application/pdf</code>	
.oth	A											The MIME type for the extension <code>.oth</code> is <code>application/vnd.oasis.opendocument.text-web</code>	
.pm	A											The MIME type for the extension <code>.pm</code> is <code>text/x-perl</code>	
.pl	A											The MIME type for the extension <code>.pl</code> is <code>text/x-perl</code>	
.atomsvc	A											The MIME type for the extension <code>.atomsvc</code> is <code>application/atomsvc+xml</code>	
.pk	A											The MIME type for the extension <code>.pk</code> is <code>application/x-tex-pk</code>	
.xul	A											The MIME type for the extension <code>.xul</code> is <code>application/vnd.mozilla.xul+xml</code>	
.pic	A											The MIME type for the extension <code>.pic</code> is <code>image/x-pict</code>	
.chm	A											The MIME type for the extension <code>.chm</code> is <code>chemical/x-chemdraw</code>	
.djv	A											The MIME type for the extension <code>.djv</code> is <code>image/vnd.djvu</code>	
.hta	A											The MIME type for the extension <code>.hta</code> is <code>application/hta</code>	
.py	A											The MIME type for the extension <code>.py</code> is <code>text/x-python</code>	
.ttf	A											The MIME type for the extension <code>.ttf</code> is <code>application/x-font-ttf</code>	
.xml	A											The MIME type for the extension <code>.xml</code> is <code>application/xml</code>	
.mopcrt	A											The MIME type for the extension <code>.mopcrt</code> is <code>chemical/x-mopac-input</code>	
.ps	A											The MIME type for the extension <code>.ps</code> is <code>application/postscript</code>	
.htm	A											The MIME type for the extension <code>.htm</code> is <code>text/html</code>	
.m2a	A											The MIME type for the extension <code>.m2a</code> is <code>audio/mpeg</code>	
.fig	A											The MIME type for the extension <code>.fig</code> is <code>application/x-xfig</code>	
.sig	A											The MIME type for the extension <code>.sig</code> is <code>application/pgp-signature</code>	
.sid	A											The MIME type for the extension <code>.sid</code> is <code>audio/prs.sid</code>	
.cab	A											The MIME type for the extension <code>.cab</code> is <code>application/vnd.ms-cab-compressed</code>	
.tsv	A											The MIME type for the extension <code>.tsv</code> is <code>text/tab-separated-values</code>	
.xlm	A											The MIME type for the extension <code>.xlm</code> is <code>application/vnd.ms-excel</code>	
.ltx	A											The MIME type for the extension <code>.ltx</code> is <code>text/x-tex</code>	
.tsp	A											The MIME type for the extension <code>.tsp</code> is <code>application/dsptype</code>	
.ltf	A											The MIME type for the extension <code>.ltf</code> is <code>application/vnd.frogans.ltf</code>	
.xlb	A											The MIME type for the extension <code>.xlb</code> is <code>application/vnd.ms-excel</code>	
.prc	A											The MIME type for the extension <code>.prc</code> is <code>application/x-mobipocket-ebook</code>	
.pre	A											The MIME type for the extension <code>.pre</code> is <code>application/vnd.lotus-freelance</code>	
.prf	A											The MIME type for the extension <code>.prf</code> is <code>application/pics-rules</code>	
.car	A											The MIME type for the extension <code>.car</code> is <code>application/vnd.curl.car</code>	
.dd2	A											The MIME type for the extension <code>.dd2</code> is <code>application/vnd.oma.dd2+xml</code>	
.cat	A											The MIME type for the extension <code>.cat</code> is <code>application/vnd.ms-pki.seccat</code>	
.dms	A											The MIME type for the extension <code>.dms</code> is <code>application/x-dms</code>	
.tcap	A											The MIME type for the extension <code>.tcap</code> is <code>application/vnd.3gpp2.tcap</code>	
.ief	A											The MIME type for the extension <code>.ief</code> is <code>image/ief</code>	
.mp4s	A											The MIME type for the extension <code>.mp4s</code> is <code>application/mp4</code>	
.jpgm	A											The MIME type for the extension <code>.jpgm</code> is <code>video/jpm</code>	
.c4d	A											The MIME type for the extension <code>.c4d</code> is <code>application/vnd.clonk.c4group</code>	
.c4g	A											The MIME type for the extension <code>.c4g</code> is <code>application/vnd.clonk.c4group</code>	
.c4f	A											The MIME type for the extension <code>.c4f</code> is <code>application/vnd.clonk.c4group</code>	
.texinfo	A											The MIME type for the extension <code>.texinfo</code> is <code>application/x-texinfo</code>	
.mp4a	A											The MIME type for the extension <code>.mp4a</code> is <code>audio/mp4</code>	
.dmg	A											The MIME type for the extension <code>.dmg</code> is <code>application/x-apple-diskimage</code>	
.c4p	A											The MIME type for the extension <code>.c4p</code> is <code>application/vnd.clonk.c4group</code>	
.c4u	A											The MIME type for the extension <code>.c4u</code> is <code>application/vnd.clonk.c4group</code>	
.vis	A											The MIME type for the extension <code>.vis</code> is <code>application/vnd.visionary</code>	
.viv	A											The MIME type for the extension <code>.viv</code> is <code>video/vnd.vivo</code>	
.ddd	A											The MIME type for the extension <code>.ddd</code> is <code>application/vnd.fujixerox.ddd</code>	
.link66	A											The MIME type for the extension <code>.link66</code> is <code>application/vnd.route66.link66+xml</code>	
.tmo	A											The MIME type for the extension <code>.tmo</code> is <code>application/vnd.tmobile-livetv</code>	
.ext	A											The MIME type for the extension <code>.ext</code> is <code>application/vnd.novadigm.ext</code>	
.csml	A											The MIME type for the extension <code>.csml</code> is <code>chemical/x-csml</code>	
.mus	A											The MIME type for the extension <code>.mus</code> is <code>application/vnd.musician</code>	
.opf	A											The MIME type for the extension <code>.opf</code> is <code>application/oebps-package+xml</code>	
.exe	A											The MIME type for the extension <code>.exe</code> is <code>application/x-msdownload</code>	
.xpw	A											The MIME type for the extension <code>.xpw</code> is <code>application/vnd.intercon.formnet</code>	
.wsc	A											The MIME type for the extension <code>.wsc</code> is <code>text/scriptlet</code>	
.xpr	A											The MIME type for the extension <code>.xpr</code> is <code>application/vnd.is-xpr</code>	
.xps	A											The MIME type for the extension <code>.xps</code> is <code>application/vnd.ms-xpsdocument</code>	
.dsc	A											The MIME type for the extension <code>.dsc</code> is <code>text/prs.lines.tag</code>	
.fh7	A											The MIME type for the extension <code>.fh7</code> is <code>image/x-freehand</code>	
.xpx	A											The MIME type for the extension <code>.xpx</code> is <code>application/vnd.intercon.formnet</code>	
.mscml	A											The MIME type for the extension <code>.mscml</code> is <code>application/mediaservercontrol+xml</code>	
.rep	A											The MIME type for the extension <code>.rep</code> is <code>application/vnd.businessobjects</code>	
.xpm	A											The MIME type for the extension <code>.xpm</code> is <code>image/x-xpixmap</code>	
.mpeg	A											The MIME type for the extension <code>.mpeg</code> is <code>video/mpeg</code>	
.ms	A											The MIME type for the extension <code>.ms</code> is <code>text/troff</code>	
.sisx	A											The MIME type for the extension <code>.sisx</code> is <code>application/vnd.symbian.install</code>	
.m4v	A											The MIME type for the extension <code>.m4v</code> is <code>video/x-m4v</code>	
.spq	A											The MIME type for the extension <code>.spq</code> is <code>application/scvp-vp-request</code>	
.spp	A											The MIME type for the extension <code>.spp</code> is <code>application/scvp-vp-response</code>	
.ami	A											The MIME type for the extension <code>.ami</code> is <code>application/vnd.amiga.ami</code>	
.fm	A											The MIME type for the extension <code>.fm</code> is <code>application/x-maker</code>	
.ram	A											The MIME type for the extension <code>.ram</code> is <code>audio/x-pn-realaudio</code>	
.ttc	A											The MIME type for the extension <code>.ttc</code> is <code>application/x-font-ttf</code>	
.fh	A											The MIME type for the extension <code>.fh</code> is <code>image/x-freehand</code>	
.sgml	A											The MIME type for the extension <code>.sgml</code> is <code>text/sgml</code>	
.spf	A											The MIME type for the extension <code>.spf</code> is <code>application/vnd.yamaha.smaf-phrase</code>	
.xlt	A											The MIME type for the extension <code>.xlt</code> is <code>application/vnd.ms-excel</code>	
.spc	A											The MIME type for the extension <code>.spc</code> is <code>chemical/x-galactic-spc</code>	
.spl	A											The MIME type for the extension <code>.spl</code> is <code>application/x-futuresplash</code>	
.bat	A											The MIME type for the extension <code>.bat</code> is <code>application/x-msdownload</code>	
.emma	A											The MIME type for the extension <code>.emma</code> is <code>application/emma+xml</code>	
.emb	A											The MIME type for the extension <code>.emb</code> is <code>chemical/x-embl-dl-nucleotide</code>	
.eml	A											The MIME type for the extension <code>.eml</code> is <code>message/rfc822</code>	
.for	A											The MIME type for the extension <code>.for</code> is <code>text/x-fortran</code>	
.cbin	A											The MIME type for the extension <code>.cbin</code> is <code>chemical/x-cactvs-binary</code>	
.diff	A											The MIME type for the extension <code>.diff</code> is <code>text/plain</code>	
.lvp	A											The MIME type for the extension <code>.lvp</code> is <code>audio/vnd.lucent.voice</code>	
.fhc	A											The MIME type for the extension <code>.fhc</code> is <code>image/x-freehand</code>	
.gac	A											The MIME type for the extension <code>.gac</code> is <code>application/vnd.groove-account</code>	
.cww	A											The MIME type for the extension <code>.cww</code> is <code>application/prs.cww</code>	
.gal	A											The MIME type for the extension <code>.gal</code> is <code>chemical/x-gaussian-log</code>	
.efif	A											The MIME type for the extension <code>.efif</code> is <code>application/vnd.picsel</code>	
.isp	A											The MIME type for the extension <code>.isp</code> is <code>application/x-internet-signup</code>	
.gjc	A											The MIME type for the extension <code>.gjc</code> is <code>chemical/x-gaussian-input</code>	
.wad	A											The MIME type for the extension <code>.wad</code> is <code>application/x-doom</code>	
.saf	A											The MIME type for the extension <code>.saf</code> is <code>application/vnd.yamaha.smaf-audio</code>	
.txf	A											The MIME type for the extension <code>.txf</code> is <code>application/vnd.mobius.txf</code>	
.utz	A											The MIME type for the extension <code>.utz</code> is <code>application/vnd.uiq.theme</code>	
.txd	A											The MIME type for the extension <code>.txd</code> is <code>application/vnd.genomatix.tuxedo</code>	
.m2v	A											The MIME type for the extension <code>.m2v</code> is <code>video/mpeg</code>	
.art	A											The MIME type for the extension <code>.art</code> is <code>image/x-jg</code>	
.wrl	A											The MIME type for the extension <code>.wrl</code> is <code>x-world/x-vrml</code>	
.tk	A											The MIME type for the extension <code>.tk</code> is <code>text/x-tcl</code>	
.wav	A											The MIME type for the extension <code>.wav</code> is <code>audio/x-wav</code>	
.rsd	A											The MIME type for the extension <code>.rsd</code> is <code>application/rsd+xml</code>	
.mxml	A											The MIME type for the extension <code>.mxml</code> is <code>application/xv+xml</code>	
.xsm	A											The MIME type for the extension <code>.xsm</code> is <code>application/vnd.syncml+xml</code>	
.xbm	A											The MIME type for the extension <code>.xbm</code> is <code>image/x-xbitmap</code>	
.txt	A											The MIME type for the extension <code>.txt</code> is <code>text/plain</code>	
.mb	A											The MIME type for the extension <code>.mb</code> is <code>application/mathematica</code>	
.xbd	A											The MIME type for the extension <code>.xbd</code> is <code>application/vnd.fujixerox.docuworks.binder</code>	
.wax	A											The MIME type for the extension <code>.wax</code> is <code>audio/x-ms-wax</code>	
.mlp	A											The MIME type for the extension <code>.mlp</code> is <code>application/vnd.dolby.mlp</code>	
.qxd	A											The MIME type for the extension <code>.qxd</code> is <code>application/vnd.quark.quarkxpress</code>	
.twd	A											The MIME type for the extension <code>.twd</code> is <code>application/vnd.simtech-mindmapper</code>	
.dna	A											The MIME type for the extension <code>.dna</code> is <code>application/vnd.dna</code>	
.ts	A											The MIME type for the extension <code>.ts</code> is <code>text/texmacs</code>	
.tr	A											The MIME type for the extension <code>.tr</code> is <code>text/troff</code>	
.distz	A											The MIME type for the extension <code>.distz</code> is <code>application/octet-stream</code>	
.tm	A											The MIME type for the extension <code>.tm</code> is <code>text/texmacs</code>	
.smil	A											The MIME type for the extension <code>.smil</code> is <code>application/smil+xml</code>	
.fnc	A											The MIME type for the extension <code>.fnc</code> is <code>application/vnd.frogans.fnc</code>	
.et3	A											The MIME type for the extension <code>.et3</code> is <code>application/vnd.eszigno3+xml</code>	
.xif	A											The MIME type for the extension <code>.xif</code> is <code>image/vnd.xiff</code>	
.daf	A											The MIME type for the extension <code>.daf</code> is <code>application/vnd.mobius.daf</code>	
.wmx	A											The MIME type for the extension <code>.wmx</code> is <code>video/x-ms-wmx</code>	
.old	A											The MIME type for the extension <code>.old</code> is <code>application/x-trash</code>	
.cer	A											The MIME type for the extension <code>.cer</code> is <code>chemical/x-cerius</code>	
.smf	A											The MIME type for the extension <code>.smf</code> is <code>application/vnd.stardivision.math</code>	
.ufd	A											The MIME type for the extension <code>.ufd</code> is <code>application/vnd.ufdl</code>	
.cef	A											The MIME type for the extension <code>.cef</code> is <code>chemical/x-cxf</code>	
.dotm	A											The MIME type for the extension <code>.dotm</code> is <code>application/vnd.ms-word.template.macroenabled.12</code>	
.asx	A											The MIME type for the extension <code>.asx</code> is <code>video/x-ms-asf</code>	
.smi	A											The MIME type for the extension <code>.smi</code> is <code>application/smil+xml</code>	
.bsd	A											The MIME type for the extension <code>.bsd</code> is <code>chemical/x-crossfire</code>	
.ctab	A											The MIME type for the extension <code>.ctab</code> is <code>chemical/x-cactvs-binary</code>	
.3gp	A											The MIME type for the extension <code>.3gp</code> is <code>video/3gpp</code>	
.pub	A											The MIME type for the extension <code>.pub</code> is <code>application/x-mspublisher</code>	
.inp	A											The MIME type for the extension <code>.inp</code> is <code>chemical/x-gamess-input</code>	
.jisp	A											The MIME type for the extension <code>.jisp</code> is <code>application/vnd.jisp</code>	
.sfs	A											The MIME type for the extension <code>.sfs</code> is <code>application/vnd.spotfire.sfs</code>	
.imp	A											The MIME type for the extension <code>.imp</code> is <code>application/vnd.accpac.simply.imp</code>	
.mpn	A											The MIME type for the extension <code>.mpn</code> is <code>application/vnd.mophun.application</code>	
.etx	A											The MIME type for the extension <code>.etx</code> is <code>text/x-setext</code>	
.iges	A											The MIME type for the extension <code>.iges</code> is <code>model/iges</code>	
.ogv	A											The MIME type for the extension <code>.ogv</code> is <code>video/ogg</code>	
.movie	A											The MIME type for the extension <code>.movie</code> is <code>video/x-sgi-movie</code>	
.dxp	A											The MIME type for the extension <code>.dxp</code> is <code>application/vnd.spotfire.dxp</code>	
.png	A											The MIME type for the extension <code>.png</code> is <code>image/png</code>	
.mhtml	A											The MIME type for the extension <code>.mhtml</code> is <code>message/rfc822</code>	
.tar	A											The MIME type for the extension <code>.tar</code> is <code>application/x-tar</code>	
.pnm	A											The MIME type for the extension <code>.pnm</code> is <code>image/x-portable-anymap</code>	
.taz	A											The MIME type for the extension <code>.taz</code> is <code>application/x-gtar</code>	
.mqy	A											The MIME type for the extension <code>.mqy</code> is <code>application/vnd.mobius.mqy</code>	
.asm	A											The MIME type for the extension <code>.asm</code> is <code>text/x-asm</code>	
.dxf	A											The MIME type for the extension <code>.dxf</code> is <code>image/vnd.dxf</code>	
.rnc	A											The MIME type for the extension <code>.rnc</code> is <code>application/relax-ng-compact-syntax</code>	
.tao	A											The MIME type for the extension <code>.tao</code> is <code>application/vnd.tao.intent-module-archive</code>	
.psp	A											The MIME type for the extension <code>.psp</code> is <code>text/x-psp</code>	
.323	A											The MIME type for the extension <code>.323</code> is <code>text/h323</code>	
.wvx	A											The MIME type for the extension <code>.wvx</code> is <code>video/x-ms-wvx</code>	
.gdl	A											The MIME type for the extension <code>.gdl</code> is <code>model/vnd.gdl</code>	
.hpgl	A											The MIME type for the extension <code>.hpgl</code> is <code>application/vnd.hp-hpgl</code>	
.bak	A											The MIME type for the extension <code>.bak</code> is <code>application/x-trash</code>	
.dotx	A											The MIME type for the extension <code>.dotx</code> is <code>application/vnd.openxmlformats-officedocument.wordprocessingml.template</code>	
.ecelp9600	A											The MIME type for the extension <code>.ecelp9600</code> is <code>audio/vnd.nuera.ecelp9600</code>	
.cu	A											The MIME type for the extension <code>.cu</code> is <code>application/cu-seeme</code>	
.chrt	A											The MIME type for the extension <code>.chrt</code> is <code>application/x-kchart</code>	
.wsdl	A											The MIME type for the extension <code>.wsdl</code> is <code>application/wsdl+xml</code>	
.dwg	A											The MIME type for the extension <code>.dwg</code> is <code>image/vnd.dwg</code>	
.dwf	A											The MIME type for the extension <code>.dwf</code> is <code>model/vnd.dwf</code>	
.jmz	A											The MIME type for the extension <code>.jmz</code> is <code>application/x-jmol</code>	
.fg5	A											The MIME type for the extension <code>.fg5</code> is <code>application/vnd.fujitsu.oasysgp</code>	
.oza	A											The MIME type for the extension <code>.oza</code> is <code>application/x-oz-application</code>	
.dcurl	A											The MIME type for the extension <code>.dcurl</code> is <code>text/vnd.curl.dcurl</code>	
.aif	A											The MIME type for the extension <code>.aif</code> is <code>audio/x-aiff</code>	
.ei6	A											The MIME type for the extension <code>.ei6</code> is <code>application/vnd.pg.osasli</code>	
.sty	A											The MIME type for the extension <code>.sty</code> is <code>text/x-tex</code>	
.rpm	A											The MIME type for the extension <code>.rpm</code> is <code>application/x-redhat-package-manager</code>	
.mcd	A											The MIME type for the extension <code>.mcd</code> is <code>application/vnd.mcd</code>	
.odft	A											The MIME type for the extension <code>.odft</code> is <code>application/vnd.oasis.opendocument.formula-template</code>	
.nnd	A											The MIME type for the extension <code>.nnd</code> is <code>application/vnd.noblenet-directory</code>	
.pgn	A											The MIME type for the extension <code>.pgn</code> is <code>application/x-chess-pgn</code>	
.mwf	A											The MIME type for the extension <code>.mwf</code> is <code>application/vnd.mfer</code>	
.str	A											The MIME type for the extension <code>.str</code> is <code>application/vnd.pg.format</code>	
.stw	A											The MIME type for the extension <code>.stw</code> is <code>application/vnd.sun.xml.writer.template</code>	
.sti	A											The MIME type for the extension <code>.sti</code> is <code>application/vnd.sun.xml.impress.template</code>	
.nns	A											The MIME type for the extension <code>.nns</code> is <code>application/vnd.noblenet-sealer</code>	
.stk	A											The MIME type for the extension <code>.stk</code> is <code>application/hyperstudio</code>	
.crl	A											The MIME type for the extension <code>.crl</code> is <code>application/x-pkcs7-crl</code>	
.semd	A											The MIME type for the extension <code>.semd</code> is <code>application/vnd.semd</code>	
.stl	A											The MIME type for the extension <code>.stl</code> is <code>application/vnd.ms-pki.stl</code>	
.semf	A											The MIME type for the extension <code>.semf</code> is <code>application/vnd.semf</code>	
.stc	A											The MIME type for the extension <code>.stc</code> is <code>application/vnd.sun.xml.calc.template</code>	
.crd	A											The MIME type for the extension <code>.crd</code> is <code>application/x-mscardfile</code>	
.std	A											The MIME type for the extension <code>.std</code> is <code>application/vnd.sun.xml.draw.template</code>	
.m1v	A											The MIME type for the extension <code>.m1v</code> is <code>video/mpeg</code>	
.stf	A											The MIME type for the extension <code>.stf</code> is <code>application/vnd.wt.stf</code>	
.icz	A											The MIME type for the extension <code>.icz</code> is <code>text/calendar</code>	
.gmx	A											The MIME type for the extension <code>.gmx</code> is <code>application/vnd.gmx</code>	
.ifb	A											The MIME type for the extension <code>.ifb</code> is <code>text/calendar</code>	
.hin	A											The MIME type for the extension <code>.hin</code> is <code>chemical/x-hin</code>	
.avi	A											The MIME type for the extension <code>.avi</code> is <code>video/x-msvideo</code>	
.fgd	A											The MIME type for the extension <code>.fgd</code> is <code>application/x-director</code>	
.istr	A											The MIME type for the extension <code>.istr</code> is <code>chemical/x-isostar</code>	
.cascii	A											The MIME type for the extension <code>.cascii</code> is <code>chemical/x-cactvs-binary</code>	
.dp	A											The MIME type for the extension <code>.dp</code> is <code>application/vnd.osgi.dp</code>	
.wdb	A											The MIME type for the extension <code>.wdb</code> is <code>application/vnd.ms-works</code>	
.cla	A											The MIME type for the extension <code>.cla</code> is <code>application/vnd.claymore</code>	
.pwn	A											The MIME type for the extension <code>.pwn</code> is <code>application/vnd.3m.post-it-notes</code>	
.hxx	A											The MIME type for the extension <code>.hxx</code> is <code>text/x-c++hdr</code>	
.wml	A											The MIME type for the extension <code>.wml</code> is <code>text/vnd.wap.wml</code>	
.mht	A											The MIME type for the extension <code>.mht</code> is <code>message/rfc822</code>	
.wma	A											The MIME type for the extension <code>.wma</code> is <code>audio/x-ms-wma</code>	
.wmf	A											The MIME type for the extension <code>.wmf</code> is <code>application/x-msmetafile</code>	
.wmd	A											The MIME type for the extension <code>.wmd</code> is <code>application/x-ms-wmd</code>	
.mcurl	A											The MIME type for the extension <code>.mcurl</code> is <code>text/vnd.curl.mcurl</code>	
.wmz	A											The MIME type for the extension <code>.wmz</code> is <code>application/x-ms-wmz</code>	
.clp	A											The MIME type for the extension <code>.clp</code> is <code>application/x-msclip</code>	
.cls	A											The MIME type for the extension <code>.cls</code> is <code>text/x-tex</code>	
.gamin	A											The MIME type for the extension <code>.gamin</code> is <code>chemical/x-gamess-input</code>	
.pwz	A											The MIME type for the extension <code>.pwz</code> is <code>application/vnd.ms-powerpoint</code>	
.hpp	A											The MIME type for the extension <code>.hpp</code> is <code>text/x-c++hdr</code>	
.mc1	A											The MIME type for the extension <code>.mc1</code> is <code>application/vnd.medcalcdata</code>	
.m13	A											The MIME type for the extension <code>.m13</code> is <code>application/x-msmediaview</code>	
.m14	A											The MIME type for the extension <code>.m14</code> is <code>application/x-msmediaview</code>	
.dist	A											The MIME type for the extension <code>.dist</code> is <code>application/octet-stream</code>	
.nml	A											The MIME type for the extension <code>.nml</code> is <code>application/vnd.enliven</code>	
.kpt	A											The MIME type for the extension <code>.kpt</code> is <code>application/x-kpresenter</code>	
.kpr	A											The MIME type for the extension <code>.kpr</code> is <code>application/x-kpresenter</code>	
.oa2	A											The MIME type for the extension <code>.oa2</code> is <code>application/vnd.fujitsu.oasys2</code>	
.oa3	A											The MIME type for the extension <code>.oa3</code> is <code>application/vnd.fujitsu.oasys3</code>	
.gtar	A											The MIME type for the extension <code>.gtar</code> is <code>application/x-gtar</code>	
.p7m	A											The MIME type for the extension <code>.p7m</code> is <code>application/pkcs7-mime</code>	
.nws	A											The MIME type for the extension <code>.nws</code> is <code>message/rfc822</code>	
.deb	A											The MIME type for the extension <code>.deb</code> is <code>application/x-debian-package</code>	
.p7c	A											The MIME type for the extension <code>.p7c</code> is <code>application/pkcs7-mime</code>	
.p7b	A											The MIME type for the extension <code>.p7b</code> is <code>application/x-pkcs7-certificates</code>	
.def	A											The MIME type for the extension <code>.def</code> is <code>text/plain</code>	
.mts	A											The MIME type for the extension <code>.mts</code> is <code>model/vnd.mts</code>	
.nwc	A											The MIME type for the extension <code>.nwc</code> is <code>application/x-nwc</code>	
.bpk	A											The MIME type for the extension <code>.bpk</code> is <code>application/octet-stream</code>	
.ez	A											The MIME type for the extension <code>.ez</code> is <code>application/andrew-inset</code>	
.der	A											The MIME type for the extension <code>.der</code> is <code>application/x-x509-ca-cert</code>	
.p7s	A											The MIME type for the extension <code>.p7s</code> is <code>application/pkcs7-signature</code>	
.p7r	A											The MIME type for the extension <code>.p7r</code> is <code>application/x-pkcs7-certreqresp</code>	
.ico	A											The MIME type for the extension <code>.ico</code> is <code>image/x-icon</code>	
.eps	A											The MIME type for the extension <code>.eps</code> is <code>application/postscript</code>	
.acc	A											The MIME type for the extension <code>.acc</code> is <code>application/vnd.americandynamics.acc</code>	
.bdf	A											The MIME type for the extension <code>.bdf</code> is <code>application/x-font-bdf</code>	
.ace	A											The MIME type for the extension <code>.ace</code> is <code>application/x-ace-compressed</code>	
.snf	A											The MIME type for the extension <code>.snf</code> is <code>application/x-font-snf</code>	
.rmi	A											The MIME type for the extension <code>.rmi</code> is <code>audio/midi</code>	
.listafp	A											The MIME type for the extension <code>.listafp</code> is <code>application/vnd.ibm.modcap</code>	
.dfac	A											The MIME type for the extension <code>.dfac</code> is <code>application/vnd.dreamfactory</code>	
.hs	A											The MIME type for the extension <code>.hs</code> is <code>text/x-haskell</code>	
.acu	A											The MIME type for the extension <code>.acu</code> is <code>application/vnd.acucobol</code>	
.wmlsc	A											The MIME type for the extension <code>.wmlsc</code> is <code>application/vnd.wap.wmlscriptc</code>	
.oas	A											The MIME type for the extension <code>.oas</code> is <code>application/vnd.fujitsu.oasys</code>	
.c++	A											The MIME type for the extension <code>.c++</code> is <code>text/x-c++src</code>	
.tex	A											The MIME type for the extension <code>.tex</code> is <code>text/x-tex</code>	
.wri	A											The MIME type for the extension <code>.wri</code> is <code>application/x-mswrite</code>	
.ica	A											The MIME type for the extension <code>.ica</code> is <code>application/x-ica</code>	
.mesh	A											The MIME type for the extension <code>.mesh</code> is <code>model/mesh</code>	
.irp	A											The MIME type for the extension <code>.irp</code> is <code>application/vnd.irepository.package+xml</code>	
.ecma	A											The MIME type for the extension <code>.ecma</code> is <code>application/ecmascript</code>	
.sse	A											The MIME type for the extension <code>.sse</code> is <code>application/vnd.kodak-descriptor</code>	
.ssf	A											The MIME type for the extension <code>.ssf</code> is <code>application/vnd.epson.ssf</code>	
.sitx	A											The MIME type for the extension <code>.sitx</code> is <code>application/x-stuffitx</code>	
.bdm	A											The MIME type for the extension <code>.bdm</code> is <code>application/vnd.syncml.dm+wbxml</code>	
.xpi	A											The MIME type for the extension <code>.xpi</code> is <code>application/x-xpinstall</code>	
.rtx	A											The MIME type for the extension <code>.rtx</code> is <code>text/richtext</code>	
.moc	A											The MIME type for the extension <code>.moc</code> is <code>text/x-moc</code>	
.irm	A											The MIME type for the extension <code>.irm</code> is <code>application/vnd.ibm.rights-management</code>	
.ice	A											The MIME type for the extension <code>.ice</code> is <code>x-conference/x-cooltalk</code>	
.joda	A											The MIME type for the extension <code>.joda</code> is <code>application/vnd.joost.joda-archive</code>	
.rpst	A											The MIME type for the extension <code>.rpst</code> is <code>application/vnd.nokia.radio-preset</code>	
.gsm	A											The MIME type for the extension <code>.gsm</code> is <code>audio/x-gsm</code>	
.fdf	A											The MIME type for the extension <code>.fdf</code> is <code>application/vnd.fdf</code>	
.elc	A											The MIME type for the extension <code>.elc</code> is <code>application/octet-stream</code>	
.eot	A											The MIME type for the extension <code>.eot</code> is <code>application/vnd.ms-fontobject</code>	
.rpss	A											The MIME type for the extension <code>.rpss</code> is <code>application/vnd.nokia.radio-presets</code>	
.pac	A											The MIME type for the extension <code>.pac</code> is <code>application/x-ns-proxy-autoconfig</code>	
.hps	A											The MIME type for the extension <code>.hps</code> is <code>application/vnd.hp-hps</code>	
.mfm	A											The MIME type for the extension <code>.mfm</code> is <code>application/vnd.mfmp</code>	
.wbmp	A											The MIME type for the extension <code>.wbmp</code> is <code>image/vnd.wap.wbmp</code>	
.pdb	A											The MIME type for the extension <code>.pdb</code> is <code>chemical/x-pdb</code>	
.pas	A											The MIME type for the extension <code>.pas</code> is <code>text/x-pascal</code>	
.pat	A											The MIME type for the extension <code>.pat</code> is <code>image/x-coreldrawpattern</code>	
.fbs	A											The MIME type for the extension <code>.fbs</code> is <code>image/vnd.fastbidsheet</code>	
.lrf	A											The MIME type for the extension <code>.lrf</code> is <code>application/octet-stream</code>	
.gim	A											The MIME type for the extension <code>.gim</code> is <code>application/vnd.groove-identity-message</code>	
.kfo	A											The MIME type for the extension <code>.kfo</code> is <code>application/vnd.kde.kformula</code>	
.moo	A											The MIME type for the extension <code>.moo</code> is <code>chemical/x-mopac-out</code>	
.mol	A											The MIME type for the extension <code>.mol</code> is <code>chemical/x-mdl-molfile</code>	
.ims	A											The MIME type for the extension <code>.ims</code> is <code>application/vnd.ms-ims</code>	
.gif	A											The MIME type for the extension <code>.gif</code> is <code>image/gif</code>	
.lrm	A											The MIME type for the extension <code>.lrm</code> is <code>application/vnd.ms-lrm</code>	
.cdkey	A											The MIME type for the extension <code>.cdkey</code> is <code>application/vnd.mediastation.cdkey</code>	
.atomcat	A											The MIME type for the extension <code>.atomcat</code> is <code>application/atomcat+xml</code>	
.qwd	A											The MIME type for the extension <code>.qwd</code> is <code>application/vnd.quark.quarkxpress</code>	
.shtml	A											The MIME type for the extension <code>.shtml</code> is <code>text/html</code>	
.djvu	A											The MIME type for the extension <code>.djvu</code> is <code>image/vnd.djvu</code>	
.rtf	A											The MIME type for the extension <code>.rtf</code> is <code>text/rtf</code>	
.fchk	A											The MIME type for the extension <code>.fchk</code> is <code>chemical/x-gaussian-checkpoint</code>	
.mop	A											The MIME type for the extension <code>.mop</code> is <code>chemical/x-mopac-input</code>	
.mov	A											The MIME type for the extension <code>.mov</code> is <code>video/quicktime</code>	
.xcf	A											The MIME type for the extension <code>.xcf</code> is <code>application/x-xcf</code>	
.twds	A											The MIME type for the extension <code>.twds</code> is <code>application/vnd.simtech-mindmapper</code>	
.dxr	A											The MIME type for the extension <code>.dxr</code> is <code>application/x-director</code>	
.azw	A											The MIME type for the extension <code>.azw</code> is <code>application/vnd.amazon.ebook</code>	
.qxt	A											The MIME type for the extension <code>.qxt</code> is <code>application/vnd.quark.quarkxpress</code>	
.uls	A											The MIME type for the extension <code>.uls</code> is <code>text/iuls</code>	
.azs	A											The MIME type for the extension <code>.azs</code> is <code>application/vnd.airzip.filesecure.azs</code>	
.iif	A											The MIME type for the extension <code>.iif</code> is <code>application/vnd.shana.informed.interchange</code>	
.sv4cpio	A											The MIME type for the extension <code>.sv4cpio</code> is <code>application/x-sv4cpio</code>	
.ngdat	A											The MIME type for the extension <code>.ngdat</code> is <code>application/vnd.nokia.n-gage.data</code>	
.azf	A											The MIME type for the extension <code>.azf</code> is <code>application/vnd.airzip.filesecure.azf</code>	
.com	A											The MIME type for the extension <code>.com</code> is <code>application/x-msdownload</code>	
.cac	A											The MIME type for the extension <code>.cac</code> is <code>chemical/x-cache</code>	
.wbxml	A											The MIME type for the extension <code>.wbxml</code> is <code>application/vnd.wap.wbxml</code>	
.hh	A											The MIME type for the extension <code>.hh</code> is <code>text/x-c++hdr</code>	
.mathml	A											The MIME type for the extension <code>.mathml</code> is <code>application/mathml+xml</code>	
.rmp	A											The MIME type for the extension <code>.rmp</code> is <code>audio/x-pn-realaudio-plugin</code>	
.wiz	A											The MIME type for the extension <code>.wiz</code> is <code>application/msword</code>	
.vcd	A											The MIME type for the extension <code>.vcd</code> is <code>application/x-cdlink</code>	
.vcg	A											The MIME type for the extension <code>.vcg</code> is <code>application/vnd.groove-vcard</code>	
.vcf	A											The MIME type for the extension <code>.vcf</code> is <code>text/x-vcard</code>	
.json	A											The MIME type for the extension <code>.json</code> is <code>application/json</code>	
.shf	A											The MIME type for the extension <code>.shf</code> is <code>application/shf+xml</code>	
.grv	A											The MIME type for the extension <code>.grv</code> is <code>application/vnd.groove-injector</code>	
.tpt	A											The MIME type for the extension <code>.tpt</code> is <code>application/vnd.trid.tpt</code>	
.psb	A											The MIME type for the extension <code>.psb</code> is <code>application/vnd.3gpp.pic-bw-small</code>	
.vxml	A											The MIME type for the extension <code>.vxml</code> is <code>application/voicexml+xml</code>	
.psf	A											The MIME type for the extension <code>.psf</code> is <code>application/x-font-linux-psf</code>	
.tpl	A											The MIME type for the extension <code>.tpl</code> is <code>application/vnd.groove-tool-template</code>	
.htke	A											The MIME type for the extension <code>.htke</code> is <code>application/vnd.kenameaapp</code>	
.vcx	A											The MIME type for the extension <code>.vcx</code> is <code>application/vnd.vcx</code>	
.xhtml	A											The MIME type for the extension <code>.xhtml</code> is <code>application/xhtml+xml</code>	
.midi	A											The MIME type for the extension <code>.midi</code> is <code>audio/midi</code>	
.gre	A											The MIME type for the extension <code>.gre</code> is <code>application/vnd.geometry-explorer</code>	
.odg	A											The MIME type for the extension <code>.odg</code> is <code>application/vnd.oasis.opendocument.graphics</code>	
.texi	A											The MIME type for the extension <code>.texi</code> is <code>application/x-texinfo</code>	
.oda	A											The MIME type for the extension <code>.oda</code> is <code>application/oda</code>	
.ustar	A											The MIME type for the extension <code>.ustar</code> is <code>application/x-ustar</code>	
.ssml	A											The MIME type for the extension <code>.ssml</code> is <code>application/ssml+xml</code>	
.odb	A											The MIME type for the extension <code>.odb</code> is <code>application/vnd.oasis.opendocument.database</code>	
.odm	A											The MIME type for the extension <code>.odm</code> is <code>application/vnd.oasis.opendocument.text-master</code>	
.xvm	A											The MIME type for the extension <code>.xvm</code> is <code>application/xv+xml</code>	
.see	A											The MIME type for the extension <code>.see</code> is <code>application/vnd.seemail</code>	
.odi	A											The MIME type for the extension <code>.odi</code> is <code>application/vnd.oasis.opendocument.image</code>	
.mpkg	A											The MIME type for the extension <code>.mpkg</code> is <code>application/vnd.apple.installer+xml</code>	
.odt	A											The MIME type for the extension <code>.odt</code> is <code>application/vnd.oasis.opendocument.text</code>	
.3g2	A											The MIME type for the extension <code>.3g2</code> is <code>video/3gpp2</code>	
.odp	A											The MIME type for the extension <code>.odp</code> is <code>application/vnd.oasis.opendocument.presentation</code>	
.ods	A											The MIME type for the extension <code>.ods</code> is <code>application/vnd.oasis.opendocument.spreadsheet</code>	
.key	A											The MIME type for the extension <code>.key</code> is <code>application/pgp-keys</code>	
.ser	A											The MIME type for the extension <code>.ser</code> is <code>application/java-serialized-object</code>	
.text	A											The MIME type for the extension <code>.text</code> is <code>text/plain</code>	
.ros	A											The MIME type for the extension <code>.ros</code> is <code>chemical/x-rosdal</code>	
.qt	A											The MIME type for the extension <code>.qt</code> is <code>video/quicktime</code>	
.mpm	A											The MIME type for the extension <code>.mpm</code> is <code>application/vnd.blueice.multipass</code>	
.mpc	A											The MIME type for the extension <code>.mpc</code> is <code>chemical/x-mopac-input</code>	
.mpa	A											The MIME type for the extension <code>.mpa</code> is <code>video/mpeg</code>	
.mpg	A											The MIME type for the extension <code>.mpg</code> is <code>video/mpeg</code>	
.mng	A											The MIME type for the extension <code>.mng</code> is <code>video/x-mng</code>	
.mpe	A											The MIME type for the extension <code>.mpe</code> is <code>video/mpeg</code>	
.jdx	A											The MIME type for the extension <code>.jdx</code> is <code>chemical/x-jcamp-dx</code>	
.mpy	A											The MIME type for the extension <code>.mpy</code> is <code>application/vnd.ibm.minipay</code>	
.pot	A											The MIME type for the extension <code>.pot</code> is <code>text/plain</code>	
.umj	A											The MIME type for the extension <code>.umj</code> is <code>application/vnd.umajin</code>	
.wpd	A											The MIME type for the extension <code>.wpd</code> is <code>application/wordperfect</code>	
.mpp	A											The MIME type for the extension <code>.mpp</code> is <code>application/vnd.ms-project</code>	
.tfm	A											The MIME type for the extension <code>.tfm</code> is <code>application/x-tex-tfm</code>	
.xspf	A											The MIME type for the extension <code>.xspf</code> is <code>application/xspf+xml</code>	
.nsf	A											The MIME type for the extension <code>.nsf</code> is <code>application/vnd.lotus-notes</code>	
.wmlc	A											The MIME type for the extension <code>.wmlc</code> is <code>application/vnd.wap.wmlc</code>	
.ggb	A											The MIME type for the extension <code>.ggb</code> is <code>application/vnd.geogebra.file</code>	
.pyv	A											The MIME type for the extension <code>.pyv</code> is <code>video/vnd.ms-playready.media.pyv</code>	
.dpg	A											The MIME type for the extension <code>.dpg</code> is <code>application/vnd.dpgraph</code>	
.fzs	A											The MIME type for the extension <code>.fzs</code> is <code>application/vnd.fuzzysheet</code>	
.ggt	A											The MIME type for the extension <code>.ggt</code> is <code>application/vnd.geogebra.tool</code>	
.onetoc	A											The MIME type for the extension <code>.onetoc</code> is <code>application/onenote</code>	
.mmod	A											The MIME type for the extension <code>.mmod</code> is <code>chemical/x-macromodel-input</code>	
.kon	A											The MIME type for the extension <code>.kon</code> is <code>application/vnd.kde.kontour</code>	
.karbon	A											The MIME type for the extension <code>.karbon</code> is <code>application/vnd.kde.karbon</code>	
.prt	A											The MIME type for the extension <code>.prt</code> is <code>chemical/x-ncbi-asn1-ascii</code>	
.sw	A											The MIME type for the extension <code>.sw</code> is <code>chemical/x-swissprot</code>	
.alc	A											The MIME type for the extension <code>.alc</code> is <code>chemical/x-alchemy</code>	
.gf	A											The MIME type for the extension <code>.gf</code> is <code>application/x-tex-gf</code>	
.pfx	A											The MIME type for the extension <code>.pfx</code> is <code>application/x-pkcs12</code>	
.m4a	A											The MIME type for the extension <code>.m4a</code> is <code>audio/mpeg</code>	
.jnlp	A											The MIME type for the extension <code>.jnlp</code> is <code>application/x-java-jnlp-file</code>	
.gl	A											The MIME type for the extension <code>.gl</code> is <code>video/gl</code>	
.ivp	A											The MIME type for the extension <code>.ivp</code> is <code>application/vnd.immervision-ivp</code>	
.ivu	A											The MIME type for the extension <code>.ivu</code> is <code>application/vnd.immervision-ivu</code>	
.pfr	A											The MIME type for the extension <code>.pfr</code> is <code>application/font-tdpfr</code>	
.ent	A											The MIME type for the extension <code>.ent</code> is <code>chemical/x-pdb</code>	
.swa	A											The MIME type for the extension <code>.swa</code> is <code>application/x-director</code>	
.gv	A											The MIME type for the extension <code>.gv</code> is <code>text/vnd.graphviz</code>	
.swf	A											The MIME type for the extension <code>.swf</code> is <code>application/x-shockwave-flash</code>	
.sik	A											The MIME type for the extension <code>.sik</code> is <code>application/x-trash</code>	
.mp3	A											The MIME type for the extension <code>.mp3</code> is <code>audio/mpeg</code>	
.swi	A											The MIME type for the extension <code>.swi</code> is <code>application/vnd.arastra.swi</code>	
.sldm	A											The MIME type for the extension <code>.sldm</code> is <code>application/vnd.ms-powerpoint.slide.macroenabled.12</code>	
.pfa	A											The MIME type for the extension <code>.pfa</code> is <code>application/x-font-type1</code>	
.pfb	A											The MIME type for the extension <code>.pfb</code> is <code>application/x-font-type1</code>	
.mp4	A											The MIME type for the extension <code>.mp4</code> is <code>video/mp4</code>	
.cxf	A											The MIME type for the extension <code>.cxf</code> is <code>chemical/x-cxf</code>	
.hvp	A											The MIME type for the extension <code>.hvp</code> is <code>application/vnd.yamaha.hv-voice</code>	
.rm	A											The MIME type for the extension <code>.rm</code> is <code>audio/x-pn-realaudio</code>	
.rl	A											The MIME type for the extension <code>.rl</code> is <code>application/resource-lists+xml</code>	
.pkg	A											The MIME type for the extension <code>.pkg</code> is <code>application/octet-stream</code>	
.ra	A											The MIME type for the extension <code>.ra</code> is <code>audio/x-realaudio</code>	
.sbml	A											The MIME type for the extension <code>.sbml</code> is <code>application/sbml+xml</code>	
.seed	A											The MIME type for the extension <code>.seed</code> is <code>application/vnd.fdsn.seed</code>	
.rd	A											The MIME type for the extension <code>.rd</code> is <code>chemical/x-mdl-rdfile</code>	
.book	A											The MIME type for the extension <code>.book</code> is <code>application/x-maker</code>	
.cmdf	A											The MIME type for the extension <code>.cmdf</code> is <code>chemical/x-cmdf</code>	
.wcm	A											The MIME type for the extension <code>.wcm</code> is <code>application/vnd.ms-works</code>	
.grxml	A											The MIME type for the extension <code>.grxml</code> is <code>application/srgs+xml</code>	
.rs	A											The MIME type for the extension <code>.rs</code> is <code>application/rls-services+xml</code>	
.ppam	A											The MIME type for the extension <code>.ppam</code> is <code>application/vnd.ms-powerpoint.addin.macroenabled.12</code>	
.rq	A											The MIME type for the extension <code>.rq</code> is <code>application/sparql-query</code>	
.sxg	A											The MIME type for the extension <code>.sxg</code> is <code>application/vnd.sun.xml.writer.global</code>	
.xop	A											The MIME type for the extension <code>.xop</code> is <code>application/xop+xml</code>	
.skd	A											The MIME type for the extension <code>.skd</code> is <code>application/x-koan</code>	
.sis	A											The MIME type for the extension <code>.sis</code> is <code>application/vnd.symbian.install</code>	
.h263	A											The MIME type for the extension <code>.h263</code> is <code>video/h263</code>	
.skm	A											The MIME type for the extension <code>.skm</code> is <code>application/x-koan</code>	
.h261	A											The MIME type for the extension <code>.h261</code> is <code>video/h261</code>	
.osfpvg	A											The MIME type for the extension <code>.osfpvg</code> is <code>application/vnd.yamaha.openscoreformat.osfpvg+xml</code>	
.h264	A											The MIME type for the extension <code>.h264</code> is <code>video/h264</code>	
.skt	A											The MIME type for the extension <code>.skt</code> is <code>application/x-koan</code>	
.skp	A											The MIME type for the extension <code>.skp</code> is <code>application/x-koan</code>	
.ufdl	A											The MIME type for the extension <code>.ufdl</code> is <code>application/vnd.ufdl</code>	
.jng	A											The MIME type for the extension <code>.jng</code> is <code>image/x-jng</code>	
.cct	A											The MIME type for the extension <code>.cct</code> is <code>application/x-director</code>	
.hqx	A											The MIME type for the extension <code>.hqx</code> is <code>application/mac-binhex40</code>	
.swfl	A											The MIME type for the extension <code>.swfl</code> is <code>application/x-shockwave-flash</code>	
.ksp	A											The MIME type for the extension <code>.ksp</code> is <code>application/x-kspread</code>	
.sit	A											The MIME type for the extension <code>.sit</code> is <code>application/x-stuffit</code>	
.doc	A											The MIME type for the extension <code>.doc</code> is <code>application/msword</code>	
.uu	A											The MIME type for the extension <code>.uu</code> is <code>text/x-uuencode</code>	
.wps	A											The MIME type for the extension <code>.wps</code> is <code>application/vnd.ms-works</code>	
.shar	A											The MIME type for the extension <code>.shar</code> is <code>application/x-shar</code>	
.c3d	A											The MIME type for the extension <code>.c3d</code> is <code>chemical/x-chem3d</code>	
.psd	A											The MIME type for the extension <code>.psd</code> is <code>image/x-photoshop</code>	
.ptid	A											The MIME type for the extension <code>.ptid</code> is <code>application/vnd.pvi.ptid1</code>	
.cxt	A											The MIME type for the extension <code>.cxt</code> is <code>application/x-director</code>	
.ksh	A											The MIME type for the extension <code>.ksh</code> is <code>text/plain</code>	
.ccxml	A											The MIME type for the extension <code>.ccxml</code> is <code>application/ccxml+xml</code>	
.dot	A											The MIME type for the extension <code>.dot</code> is <code>application/msword</code>	
.cdy	A											The MIME type for the extension <code>.cdy</code> is <code>application/vnd.cinderella</code>	
.cdx	A											The MIME type for the extension <code>.cdx</code> is <code>chemical/x-cdx</code>	
.slt	A											The MIME type for the extension <code>.slt</code> is <code>application/vnd.epson.salt</code>	
.dir	A											The MIME type for the extension <code>.dir</code> is <code>application/x-director</code>	
.vor	A											The MIME type for the extension <code>.vor</code> is <code>application/vnd.stardivision.writer</code>	
.ics	A											The MIME type for the extension <code>.ics</code> is <code>text/calendar</code>	
.cdr	A											The MIME type for the extension <code>.cdr</code> is <code>image/x-coreldraw</code>	
.vox	A											The MIME type for the extension <code>.vox</code> is <code>application/x-authorware-bin</code>	
.ktr	A											The MIME type for the extension <code>.ktr</code> is <code>application/vnd.kahootz</code>	
.qps	A											The MIME type for the extension <code>.qps</code> is <code>application/vnd.publishare-delta-tree</code>	
.gsf	A											The MIME type for the extension <code>.gsf</code> is <code>application/x-font-ghostscript</code>	
.icm	A											The MIME type for the extension <code>.icm</code> is <code>application/vnd.iccprofile</code>	
.uoml	A											The MIME type for the extension <code>.uoml</code> is <code>application/vnd.uoml+xml</code>	
.icc	A											The MIME type for the extension <code>.icc</code> is <code>application/vnd.iccprofile</code>	
.ktz	A											The MIME type for the extension <code>.ktz</code> is <code>application/vnd.kahootz</code>	
.uri	A											The MIME type for the extension <code>.uri</code> is <code>text/uri-list</code>	
.cdf	A											The MIME type for the extension <code>.cdf</code> is <code>application/x-netcdf</code>	
.ai	A											The MIME type for the extension <code>.ai</code> is <code>application/postscript</code>	
.deploy	A											The MIME type for the extension <code>.deploy</code> is <code>application/octet-stream</code>	
.mp4v	A											The MIME type for the extension <code>.mp4v</code> is <code>video/mp4</code>	
.cdbcmsg	A											The MIME type for the extension <code>.cdbcmsg</code> is <code>application/vnd.contact.cmsg</code>	
.list	A											The MIME type for the extension <code>.list</code> is <code>text/plain</code>	
.esf	A											The MIME type for the extension <code>.esf</code> is <code>application/vnd.epson.esf</code>	
.abw	A											The MIME type for the extension <code>.abw</code> is <code>application/x-abiword</code>	
.wspolicy	A											The MIME type for the extension <code>.wspolicy</code> is <code>application/wspolicy+xml</code>	
.mpga	A											The MIME type for the extension <code>.mpga</code> is <code>audio/mpeg</code>	
.epub	A											The MIME type for the extension <code>.epub</code> is <code>application/epub+zip</code>	
.pki	A											The MIME type for the extension <code>.pki</code> is <code>application/pkixcmp</code>	
.cc	A											The MIME type for the extension <code>.cc</code> is <code>text/x-c++src</code>	
.hdf	A											The MIME type for the extension <code>.hdf</code> is <code>application/x-hdf</code>	
.davmount	A											The MIME type for the extension <code>.davmount</code> is <code>application/davmount+xml</code>	
.xpdl	A											The MIME type for the extension <code>.xpdl</code> is <code>application/xml</code>	
.src	A											The MIME type for the extension <code>.src</code> is <code>application/x-wais-source</code>	
.zaz	A											The MIME type for the extension <code>.zaz</code> is <code>application/vnd.zzazz.deck+xml</code>	
.wqd	A											The MIME type for the extension <code>.wqd</code> is <code>application/vnd.wqd</code>	
.log	A											The MIME type for the extension <code>.log</code> is <code>text/plain</code>	
.cxx	A											The MIME type for the extension <code>.cxx</code> is <code>text/x-c++src</code>	
.srx	A											The MIME type for the extension <code>.srx</code> is <code>application/sparql-results+xml</code>	
.box	A											The MIME type for the extension <code>.box</code> is <code>application/vnd.previewsystems.box</code>	
.boz	A											The MIME type for the extension <code>.boz</code> is <code>application/x-bzip2</code>	
.vcs	A											The MIME type for the extension <code>.vcs</code> is <code>text/x-vcalendar</code>	
.oxt	A											The MIME type for the extension <code>.oxt</code> is <code>application/vnd.openofficeorg.extension</code>	
.pbd	A											The MIME type for the extension <code>.pbd</code> is <code>application/vnd.powerbuilder6</code>	
.bh2	A											The MIME type for the extension <code>.bh2</code> is <code>application/vnd.fujitsu.oasysprs</code>	
.h++	A											The MIME type for the extension <code>.h++</code> is <code>text/x-c++hdr</code>	
.mpg4	A											The MIME type for the extension <code>.mpg4</code> is <code>video/mp4</code>	
.pptx	A											The MIME type for the extension <code>.pptx</code> is <code>application/vnd.openxmlformats-officedocument.presentationml.presentation</code>	
.gcd	A											The MIME type for the extension <code>.gcd</code> is <code>text/x-pcs-gcd</code>	
.pbm	A											The MIME type for the extension <code>.pbm</code> is <code>image/x-portable-bitmap</code>	
.gcf	A											The MIME type for the extension <code>.gcf</code> is <code>application/x-graphing-calculator</code>	
.es3	A											The MIME type for the extension <code>.es3</code> is <code>application/vnd.eszigno3+xml</code>	
.qbo	A											The MIME type for the extension <code>.qbo</code> is <code>application/vnd.intu.qbo</code>	
.vrml	A											The MIME type for the extension <code>.vrml</code> is <code>x-world/x-vrml</code>	
.msty	A											The MIME type for the extension <code>.msty</code> is <code>application/vnd.muvee.style</code>	
.dtd	A											The MIME type for the extension <code>.dtd</code> is <code>application/xml-dtd</code>	
.gcg	A											The MIME type for the extension <code>.gcg</code> is <code>chemical/x-gcg8-sequence</code>	
.dtb	A											The MIME type for the extension <code>.dtb</code> is <code>application/x-dtbook+xml</code>	
.pclxl	A											The MIME type for the extension <code>.pclxl</code> is <code>application/vnd.hp-pclxl</code>	
.xdp	A											The MIME type for the extension <code>.xdp</code> is <code>application/vnd.adobe.xdp+xml</code>	
.xdw	A											The MIME type for the extension <code>.xdw</code> is <code>application/vnd.fujixerox.docuworks</code>	
.onepkg	A											The MIME type for the extension <code>.onepkg</code> is <code>application/onenote</code>	
.mobi	A											The MIME type for the extension <code>.mobi</code> is <code>application/x-mobipocket-ebook</code>	
.mbk	A											The MIME type for the extension <code>.mbk</code> is <code>application/vnd.mobius.mbk</code>	
.cdxml	A											The MIME type for the extension <code>.cdxml</code> is <code>application/vnd.chemdraw+xml</code>	
.java	A											The MIME type for the extension <code>.java</code> is <code>text/x-java-source</code>	
.kar	A											The MIME type for the extension <code>.kar</code> is <code>audio/midi</code>	
.ipk	A											The MIME type for the extension <code>.ipk</code> is <code>application/vnd.shana.informed.package</code>	
.org	A											The MIME type for the extension <code>.org</code> is <code>application/vnd.lotus-organizer</code>	
.tiff	A											The MIME type for the extension <code>.tiff</code> is <code>image/tiff</code>	
.xslt	A											The MIME type for the extension <code>.xslt</code> is <code>application/xslt+xml</code>	
.aiff	A											The MIME type for the extension <code>.aiff</code> is <code>audio/x-aiff</code>	
.vrm	A											The MIME type for the extension <code>.vrm</code> is <code>x-world/x-vrml</code>	
.aifc	A											The MIME type for the extension <code>.aifc</code> is <code>audio/x-aiff</code>	
.xdm	A											The MIME type for the extension <code>.xdm</code> is <code>application/vnd.syncml.dm+xml</code>	
.gqf	A											The MIME type for the extension <code>.gqf</code> is <code>application/vnd.grafeq</code>	
.crt	A											The MIME type for the extension <code>.crt</code> is <code>application/x-x509-ca-cert</code>	
.flx	A											The MIME type for the extension <code>.flx</code> is <code>text/vnd.fmi.flexstor</code>	
.fly	A											The MIME type for the extension <code>.fly</code> is <code>text/vnd.fly</code>	
.kne	A											The MIME type for the extension <code>.kne</code> is <code>application/vnd.kinar</code>	
.edx	A											The MIME type for the extension <code>.edx</code> is <code>application/vnd.novadigm.edx</code>	
.flv	A											The MIME type for the extension <code>.flv</code> is <code>video/x-flv</code>	
.flw	A											The MIME type for the extension <code>.flw</code> is <code>application/vnd.kde.kivio</code>	
.susp	A											The MIME type for the extension <code>.susp</code> is <code>application/vnd.sus-calendar</code>	
.ez3	A											The MIME type for the extension <code>.ez3</code> is <code>application/vnd.ezpix-package</code>	
.ez2	A											The MIME type for the extension <code>.ez2</code> is <code>application/vnd.ezpix-album</code>	
.gqs	A											The MIME type for the extension <code>.gqs</code> is <code>application/vnd.grafeq</code>	
.hbci	A											The MIME type for the extension <code>.hbci</code> is <code>application/vnd.hbci</code>	
.ins	A											The MIME type for the extension <code>.ins</code> is <code>application/x-internet-signup</code>	
.pkipath	A											The MIME type for the extension <code>.pkipath</code> is <code>application/pkix-pkipath</code>	
.lzx	A											The MIME type for the extension <code>.lzx</code> is <code>application/x-lzx</code>	
.odc	A											The MIME type for the extension <code>.odc</code> is <code>application/vnd.oasis.opendocument.chart</code>	
.sema	A											The MIME type for the extension <code>.sema</code> is <code>application/vnd.sema</code>	
.ppt	A											The MIME type for the extension <code>.ppt</code> is <code>application/vnd.ms-powerpoint</code>	
.zmt	A											The MIME type for the extension <code>.zmt</code> is <code>chemical/x-mopac-input</code>	
.pps	A											The MIME type for the extension <code>.pps</code> is <code>application/vnd.ms-powerpoint</code>	
.ppm	A											The MIME type for the extension <code>.ppm</code> is <code>image/x-portable-pixmap</code>	
.lzh	A											The MIME type for the extension <code>.lzh</code> is <code>application/x-lzh</code>	
.latex	A											The MIME type for the extension <code>.latex</code> is <code>application/x-latex</code>	
.ppd	A											The MIME type for the extension <code>.ppd</code> is <code>application/vnd.cups-ppd</code>	
.cgm	A											The MIME type for the extension <code>.cgm</code> is <code>image/cgm</code>	
.ppa	A											The MIME type for the extension <code>.ppa</code> is <code>application/vnd.ms-powerpoint</code>	
.fpx	A											The MIME type for the extension <code>.fpx</code> is <code>image/vnd.fpx</code>	
.igl	A											The MIME type for the extension <code>.igl</code> is <code>application/vnd.igloader</code>	
.mbox	A											The MIME type for the extension <code>.mbox</code> is <code>application/mbox</code>	
.lwp	A											The MIME type for the extension <code>.lwp</code> is <code>application/vnd.lotus-wordpro</code>	
.osf	A											The MIME type for the extension <code>.osf</code> is <code>application/vnd.yamaha.openscoreformat</code>	
.frm	A											The MIME type for the extension <code>.frm</code> is <code>application/x-maker</code>	
.kwt	A											The MIME type for the extension <code>.kwt</code> is <code>application/x-kword</code>	
.dcr	A											The MIME type for the extension <code>.dcr</code> is <code>application/x-director</code>	
.oti	A											The MIME type for the extension <code>.oti</code> is <code>application/vnd.oasis.opendocument.image-template</code>	
.mp2a	A											The MIME type for the extension <code>.mp2a</code> is <code>audio/mpeg</code>	
.igx	A											The MIME type for the extension <code>.igx</code> is <code>application/vnd.micrografx.igx</code>	
.kwd	A											The MIME type for the extension <code>.kwd</code> is <code>application/x-kword</code>	
.igs	A											The MIME type for the extension <code>.igs</code> is <code>model/iges</code>	
.apr	A											The MIME type for the extension <code>.apr</code> is <code>application/vnd.lotus-approach</code>	
.npx	A											The MIME type for the extension <code>.npx</code> is <code>image/vnd.net-fpx</code>	
.n-gage	A											The MIME type for the extension <code>.n-gage</code> is <code>application/vnd.nokia.n-gage.symbian.install</code>	
.tcl	A											The MIME type for the extension <code>.tcl</code> is <code>text/x-tcl</code>	
.ogx	A											The MIME type for the extension <code>.ogx</code> is <code>application/ogg</code>	
.qtl	A											The MIME type for the extension <code>.qtl</code> is <code>application/x-quicktimeplayer</code>	
.flo	A											The MIME type for the extension <code>.flo</code> is <code>application/vnd.micrografx.flo</code>	
.jam	A											The MIME type for the extension <code>.jam</code> is <code>application/vnd.jam</code>	
.rlc	A											The MIME type for the extension <code>.rlc</code> is <code>image/vnd.fujixerox.edmics-rlc</code>	
.rld	A											The MIME type for the extension <code>.rld</code> is <code>application/resource-lists-diff+xml</code>	
.svgz	A											The MIME type for the extension <code>.svgz</code> is <code>image/svg+xml</code>	
.bz2	A											The MIME type for the extension <code>.bz2</code> is <code>application/x-bzip2</code>	
.xar	A											The MIME type for the extension <code>.xar</code> is <code>application/vnd.xara</code>	
.jar	A											The MIME type for the extension <code>.jar</code> is <code>application/java-archive</code>	
.fch	A											The MIME type for the extension <code>.fch</code> is <code>chemical/x-gaussian-checkpoint</code>	
.cpt	A											The MIME type for the extension <code>.cpt</code> is <code>image/x-corelphotopaint</code>	
.ogg	A											The MIME type for the extension <code>.ogg</code> is <code>audio/ogg</code>	
.dts	A											The MIME type for the extension <code>.dts</code> is <code>audio/vnd.dts</code>	
.oga	A											The MIME type for the extension <code>.oga</code> is <code>audio/ogg</code>	
.afp	A											The MIME type for the extension <code>.afp</code> is <code>application/vnd.ibm.modcap</code>	
.f90	A											The MIME type for the extension <code>.f90</code> is <code>text/x-fortran</code>	
.application	A											The MIME type for the extension <code>.application</code> is <code>application/x-ms-application</code>	
.mxf	A											The MIME type for the extension <code>.mxf</code> is <code>application/mxf</code>	
.rgb	A											The MIME type for the extension <code>.rgb</code> is <code>image/x-rgb</code>	
.mxl	A											The MIME type for the extension <code>.mxl</code> is <code>application/vnd.recordare.musicxml</code>	
.mxs	A											The MIME type for the extension <code>.mxs</code> is <code>application/vnd.triscape.mxs</code>	
.gram	A											The MIME type for the extension <code>.gram</code> is <code>application/srgs</code>	
.me	A											The MIME type for the extension <code>.me</code> is <code>text/troff</code>	
.jlt	A											The MIME type for the extension <code>.jlt</code> is <code>application/vnd.hp-jlyt</code>	
.mxu	A											The MIME type for the extension <code>.mxu</code> is <code>video/vnd.mpegurl</code>	
.ma	A											The MIME type for the extension <code>.ma</code> is <code>application/mathematica</code>	
.qam	A											The MIME type for the extension <code>.qam</code> is <code>application/vnd.epson.quickanime</code>	
.mm	A											The MIME type for the extension <code>.mm</code> is <code>application/x-freemind</code>	
.afm	A											The MIME type for the extension <code>.afm</code> is <code>application/x-font-type1</code>	
.mseed	A											The MIME type for the extension <code>.mseed</code> is <code>application/vnd.fdsn.mseed</code>	
.fli	A											The MIME type for the extension <code>.fli</code> is <code>video/x-fli</code>	
.dl	A											The MIME type for the extension <code>.dl</code> is <code>video/dl</code>	
.pcurl	A											The MIME type for the extension <code>.pcurl</code> is <code>application/vnd.curl.pcurl</code>	
.u32	A											The MIME type for the extension <code>.u32</code> is <code>application/x-authorware-bin</code>	
.xlsx	A											The MIME type for the extension <code>.xlsx</code> is <code>application/vnd.openxmlformats-officedocument.spreadsheetml.sheet</code>	
.vsf	A											The MIME type for the extension <code>.vsf</code> is <code>application/vnd.vsf</code>	
.res	A											The MIME type for the extension <code>.res</code> is <code>application/x-dtbresource+xml</code>	
.pgp	A											The MIME type for the extension <code>.pgp</code> is <code>application/pgp-signature</code>	
.xlsb	A											The MIME type for the extension <code>.xlsb</code> is <code>application/vnd.ms-excel.sheet.binary.macroenabled.12</code>	
.nlu	A											The MIME type for the extension <code>.nlu</code> is <code>application/vnd.neurolanguage.nlu</code>	
.pgm	A											The MIME type for the extension <code>.pgm</code> is <code>image/x-portable-graymap</code>	
.xyz	A											The MIME type for the extension <code>.xyz</code> is <code>chemical/x-xyz</code>	
.svg	A											The MIME type for the extension <code>.svg</code> is <code>image/svg+xml</code>	
.svd	A											The MIME type for the extension <code>.svd</code> is <code>application/vnd.svd</code>	
.atom	A											The MIME type for the extension <code>.atom</code> is <code>application/atom+xml</code>	
.qfx	A											The MIME type for the extension <code>.qfx</code> is <code>application/vnd.intu.qfx</code>	
.roff	A											The MIME type for the extension <code>.roff</code> is <code>text/troff</code>	
.unityweb	A											The MIME type for the extension <code>.unityweb</code> is <code>application/vnd.unity</code>	
.123	A											The MIME type for the extension <code>.123</code> is <code>application/vnd.lotus-1-2-3</code>	
.xlsm	A											The MIME type for the extension <code>.xlsm</code> is <code>application/vnd.ms-excel.sheet.macroenabled.12</code>	
.xbap	A											The MIME type for the extension <code>.xbap</code> is <code>application/x-ms-xbap</code>	
.scurl	A											The MIME type for the extension <code>.scurl</code> is <code>text/vnd.curl.scurl</code>	
.cub	A											The MIME type for the extension <code>.cub</code> is <code>chemical/x-gaussian-cube</code>	
.eol	A											The MIME type for the extension <code>.eol</code> is <code>audio/vnd.digital-winds</code>	
.frame	A											The MIME type for the extension <code>.frame</code> is <code>application/x-maker</code>	
.mol2	A											The MIME type for the extension <code>.mol2</code> is <code>chemical/x-mol2</code>	
.lostxml	A											The MIME type for the extension <code>.lostxml</code> is <code>application/lost+xml</code>	
.spx	A											The MIME type for the extension <code>.spx</code> is <code>audio/ogg</code>	
.man	A											The MIME type for the extension <code>.man</code> is <code>text/troff</code>	
.gau	A											The MIME type for the extension <code>.gau</code> is <code>chemical/x-gaussian-input</code>	
.dat	A											The MIME type for the extension <code>.dat</code> is <code>chemical/x-mopac-input</code>	
.fvt	A											The MIME type for the extension <code>.fvt</code> is <code>video/vnd.fvt</code>	
.mag	A											The MIME type for the extension <code>.mag</code> is <code>application/vnd.ecowin.chart</code>	
.pya	A											The MIME type for the extension <code>.pya</code> is <code>audio/vnd.ms-playready.media.pya</code>	
.lsf	A											The MIME type for the extension <code>.lsf</code> is <code>video/x-la-asf</code>	
.pyc	A											The MIME type for the extension <code>.pyc</code> is <code>application/x-python-code</code>	
.atx	A											The MIME type for the extension <code>.atx</code> is <code>application/vnd.antix.game-component</code>	
.dataless	A											The MIME type for the extension <code>.dataless</code> is <code>application/vnd.fdsn.seed</code>	
.apk	A											The MIME type for the extension <code>.apk</code> is <code>application/vnd.android.package-archive</code>	
.mny	A											The MIME type for the extension <code>.mny</code> is <code>application/x-msmoney</code>	
.iii	A											The MIME type for the extension <code>.iii</code> is <code>application/x-iphone</code>	
.pyo	A											The MIME type for the extension <code>.pyo</code> is <code>application/x-python-code</code>	
.ghf	A											The MIME type for the extension <code>.ghf</code> is <code>application/vnd.groove-help</code>	
.cpio	A											The MIME type for the extension <code>.cpio</code> is <code>application/x-cpio</code>	
.setreg	A											The MIME type for the extension <code>.setreg</code> is <code>application/set-registration-initiation</code>	
.atc	A											The MIME type for the extension <code>.atc</code> is <code>application/vnd.acucorp</code>	
.lsx	A											The MIME type for the extension <code>.lsx</code> is <code>video/x-la-asf</code>	
.ecelp4800	A											The MIME type for the extension <code>.ecelp4800</code> is <code>audio/vnd.nuera.ecelp4800</code>	
.val	A											The MIME type for the extension <code>.val</code> is <code>chemical/x-ncbi-asn1-binary</code>	
.dll	A											The MIME type for the extension <code>.dll</code> is <code>application/x-msdownload</code>	
.sd2	A											The MIME type for the extension <code>.sd2</code> is <code>audio/x-sd2</code>	
.rif	A											The MIME type for the extension <code>.rif</code> is <code>application/reginfo+xml</code>	
.sct	A											The MIME type for the extension <code>.sct</code> is <code>text/scriptlet</code>	
.scq	A											The MIME type for the extension <code>.scq</code> is <code>application/scvp-cv-request</code>	
.scs	A											The MIME type for the extension <code>.scs</code> is <code>application/scvp-cv-response</code>	
.scm	A											The MIME type for the extension <code>.scm</code> is <code>application/vnd.lotus-screencam</code>	
.xfdl	A											The MIME type for the extension <code>.xfdl</code> is <code>application/vnd.xfdl</code>	
.scd	A											The MIME type for the extension <code>.scd</code> is <code>application/x-msschedule</code>	
.cdt	A											The MIME type for the extension <code>.cdt</code> is <code>image/x-coreldrawtemplate</code>	
.xfdf	A											The MIME type for the extension <code>.xfdf</code> is <code>application/vnd.adobe.xfdf</code>	
.xwd	A											The MIME type for the extension <code>.xwd</code> is <code>image/x-xwindowdump</code>	
.xlam	A											The MIME type for the extension <code>.xlam</code> is <code>application/vnd.ms-excel.addin.macroenabled.12</code>	
.aab	A											The MIME type for the extension <code>.aab</code> is <code>application/x-authorware-bin</code>	
.aac	A											The MIME type for the extension <code>.aac</code> is <code>audio/x-aac</code>	
.sda	A											The MIME type for the extension <code>.sda</code> is <code>application/vnd.stardivision.draw</code>	
.aam	A											The MIME type for the extension <code>.aam</code> is <code>application/x-authorware-map</code>	
.sdc	A											The MIME type for the extension <code>.sdc</code> is <code>application/vnd.stardivision.calc</code>	
.g3	A											The MIME type for the extension <code>.g3</code> is <code>image/g3fax</code>	
.sdd	A											The MIME type for the extension <code>.sdd</code> is <code>application/vnd.stardivision.impress</code>	
.sdf	A											The MIME type for the extension <code>.sdf</code> is <code>chemical/x-mdl-sdfile</code>	
.js	A											The MIME type for the extension <code>.js</code> is <code>application/x-javascript</code>	
.aas	A											The MIME type for the extension <code>.aas</code> is <code>application/x-authorware-seg</code>	
.sdp	A											The MIME type for the extension <code>.sdp</code> is <code>application/vnd.stardivision.impress</code>	
.sdw	A											The MIME type for the extension <code>.sdw</code> is <code>application/vnd.stardivision.writer</code>	
.plb	A											The MIME type for the extension <code>.plb</code> is <code>application/vnd.3gpp.pic-bw-large</code>	
.plc	A											The MIME type for the extension <code>.plc</code> is <code>application/vnd.mobius.plc</code>	
.plf	A											The MIME type for the extension <code>.plf</code> is <code>application/vnd.pocketlearn</code>	
.tgz	A											The MIME type for the extension <code>.tgz</code> is <code>application/x-gtar</code>	
.lha	A											The MIME type for the extension <code>.lha</code> is <code>application/x-lha</code>	
.edm	A											The MIME type for the extension <code>.edm</code> is <code>application/vnd.novadigm.edm</code>	
.sxd	A											The MIME type for the extension <code>.sxd</code> is <code>application/vnd.sun.xml.draw</code>	
.wtb	A											The MIME type for the extension <code>.wtb</code> is <code>application/vnd.webturbo</code>	
.msf	A											The MIME type for the extension <code>.msf</code> is <code>application/vnd.epson.msf</code>	
.pls	A											The MIME type for the extension <code>.pls</code> is <code>audio/x-scpls</code>	
.knp	A											The MIME type for the extension <code>.knp</code> is <code>application/vnd.kinar</code>	
.tgf	A											The MIME type for the extension <code>.tgf</code> is <code>chemical/x-mdl-tgf</code>	
.3dml	A											The MIME type for the extension <code>.3dml</code> is <code>text/vnd.in3d.3dml</code>	
.lhs	A											The MIME type for the extension <code>.lhs</code> is <code>text/x-literate-haskell</code>	
.msl	A											The MIME type for the extension <code>.msl</code> is <code>application/vnd.mobius.msl</code>	
.mpt	A											The MIME type for the extension <code>.mpt</code> is <code>application/vnd.ms-project</code>	
.qxb	A											The MIME type for the extension <code>.qxb</code> is <code>application/vnd.quark.quarkxpress</code>	
.msh	A											The MIME type for the extension <code>.msh</code> is <code>model/mesh</code>	
.msi	A											The MIME type for the extension <code>.msi</code> is <code>application/x-msi</code>	
.oprc	A											The MIME type for the extension <code>.oprc</code> is <code>application/vnd.palm</code>	
.cpa	A											The MIME type for the extension <code>.cpa</code> is <code>chemical/x-compass</code>	
.pcf	A											The MIME type for the extension <code>.pcf</code> is <code>application/x-font-pcf</code>	
.xla	A											The MIME type for the extension <code>.xla</code> is <code>application/vnd.ms-excel</code>	
.potm	A											The MIME type for the extension <code>.potm</code> is <code>application/vnd.ms-powerpoint.template.macroenabled.12</code>	
.pcl	A											The MIME type for the extension <code>.pcl</code> is <code>application/vnd.hp-pcl</code>	
.jpe	A											The MIME type for the extension <code>.jpe</code> is <code>image/jpeg</code>	
.jpg	A											The MIME type for the extension <code>.jpg</code> is <code>image/jpeg</code>	
.rxn	A											The MIME type for the extension <code>.rxn</code> is <code>chemical/x-mdl-rxnfile</code>	
.mdb	A											The MIME type for the extension <code>.mdb</code> is <code>application/x-msaccess</code>	
.cmp	A											The MIME type for the extension <code>.cmp</code> is <code>application/vnd.yellowriver-custom-menu</code>	
.potx	A											The MIME type for the extension <code>.potx</code> is <code>application/vnd.openxmlformats-officedocument.presentationml.template</code>	
.jpm	A											The MIME type for the extension <code>.jpm</code> is <code>video/jpm</code>	
.pcx	A											The MIME type for the extension <code>.pcx</code> is <code>image/x-pcx</code>	
.zir	A											The MIME type for the extension <code>.zir</code> is <code>application/vnd.zul</code>	
.mdi	A											The MIME type for the extension <code>.mdi</code> is <code>image/vnd.ms-modi</code>	
.zip	A											The MIME type for the extension <code>.zip</code> is <code>application/zip</code>	
.xtel	A											The MIME type for the extension <code>.xtel</code> is <code>chemical/x-xtel</code>	
.vss	A											The MIME type for the extension <code>.vss</code> is <code>application/vnd.visio</code>	
.adp	A											The MIME type for the extension <code>.adp</code> is <code>audio/adpcm</code>	
.clkk	A											The MIME type for the extension <code>.clkk</code> is <code>application/vnd.crick.clicker.keyboard</code>	
.ppsx	A											The MIME type for the extension <code>.ppsx</code> is <code>application/vnd.openxmlformats-officedocument.presentationml.slideshow</code>	
.vsw	A											The MIME type for the extension <code>.vsw</code> is <code>application/vnd.visio</code>	
.class	A											The MIME type for the extension <code>.class</code> is <code>application/java-vm</code>	
.torrent	A											The MIME type for the extension <code>.torrent</code> is <code>application/x-bittorrent</code>	
.mmf	A											The MIME type for the extension <code>.mmf</code> is <code>application/vnd.smaf</code>	
.mj2	A											The MIME type for the extension <code>.mj2</code> is <code>video/mj2</code>	
.m3a	A											The MIME type for the extension <code>.m3a</code> is <code>audio/mpeg</code>	
.xer	A											The MIME type for the extension <code>.xer</code> is <code>application/patch-ops-error+xml</code>	
.asc	A											The MIME type for the extension <code>.asc</code> is <code>text/plain</code>	
.qxl	A											The MIME type for the extension <code>.qxl</code> is <code>application/vnd.quark.quarkxpress</code>	
.fh4	A											The MIME type for the extension <code>.fh4</code> is <code>image/x-freehand</code>	
.vsd	A											The MIME type for the extension <code>.vsd</code> is <code>application/vnd.visio</code>	
.clkx	A											The MIME type for the extension <code>.clkx</code> is <code>application/vnd.crick.clicker</code>	
.clkw	A											The MIME type for the extension <code>.clkw</code> is <code>application/vnd.crick.clicker.wordbank</code>	
.xo	A											The MIME type for the extension <code>.xo</code> is <code>application/vnd.olpc-sugar</code>	
.clkt	A											The MIME type for the extension <code>.clkt</code> is <code>application/vnd.crick.clicker.template</code>	
.asn	A											The MIME type for the extension <code>.asn</code> is <code>chemical/x-ncbi-asn1-spec</code>	
.aso	A											The MIME type for the extension <code>.aso</code> is <code>chemical/x-ncbi-asn1-binary</code>	
.mmr	A											The MIME type for the extension <code>.mmr</code> is <code>image/vnd.fujixerox.edmics-mmr</code>	
.clkp	A											The MIME type for the extension <code>.clkp</code> is <code>application/vnd.crick.clicker.palette</code>	
.sh	A											The MIME type for the extension <code>.sh</code> is <code>text/x-sh</code>	
.hvd	A											The MIME type for the extension <code>.hvd</code> is <code>application/vnd.yamaha.hv-dic</code>	
.so	A											The MIME type for the extension <code>.so</code> is <code>application/octet-stream</code>	
.wbs	A											The MIME type for the extension <code>.wbs</code> is <code>application/vnd.criticaltools.wbs+xml</code>	
.xlc	A											The MIME type for the extension <code>.xlc</code> is <code>application/vnd.ms-excel</code>	
.sc	A											The MIME type for the extension <code>.sc</code> is <code>application/vnd.ibm.secure-container</code>	
.sd	A											The MIME type for the extension <code>.sd</code> is <code>chemical/x-mdl-sdfile</code>	
.cif	A											The MIME type for the extension <code>.cif</code> is <code>chemical/x-cif</code>	
.dic	A											The MIME type for the extension <code>.dic</code> is <code>text/x-c</code>	
.xls	A											The MIME type for the extension <code>.xls</code> is <code>application/vnd.ms-excel</code>	
.cii	A											The MIME type for the extension <code>.cii</code> is <code>application/vnd.anser-web-certificate-issue-initiation</code>	
.xlw	A											The MIME type for the extension <code>.xlw</code> is <code>application/vnd.ms-excel</code>	
.cil	A											The MIME type for the extension <code>.cil</code> is <code>application/vnd.ms-artgalry</code>	
.fb	A											The MIME type for the extension <code>.fb</code> is <code>application/x-maker</code>	
.wmls	A											The MIME type for the extension <code>.wmls</code> is <code>text/vnd.wap.wmlscript</code>	
.wks	A											The MIME type for the extension <code>.wks</code> is <code>application/vnd.ms-works</code>	
.wmv	A											The MIME type for the extension <code>.wmv</code> is <code>video/x-ms-wmv</code>	
.gtw	A											The MIME type for the extension <code>.gtw</code> is <code>model/vnd.gtw</code>	
.dx	A											The MIME type for the extension <code>.dx</code> is <code>chemical/x-jcamp-dx</code>	
.gtm	A											The MIME type for the extension <code>.gtm</code> is <code>application/vnd.groove-tool-message</code>	
.trm	A											The MIME type for the extension <code>.trm</code> is <code>application/x-msterminal</code>	
.ppsm	A											The MIME type for the extension <code>.ppsm</code> is <code>application/vnd.ms-powerpoint.slideshow.macroenabled.12</code>	
.kin	A											The MIME type for the extension <code>.kin</code> is <code>chemical/x-kinemage</code>	
.urls	A											The MIME type for the extension <code>.urls</code> is <code>text/uri-list</code>	
.kil	A											The MIME type for the extension <code>.kil</code> is <code>application/x-killustrator</code>	
.fbdoc	A											The MIME type for the extension <code>.fbdoc</code> is <code>application/x-maker</code>	
.html	A											The MIME type for the extension <code>.html</code> is <code>text/html</code>	
.kia	A											The MIME type for the extension <code>.kia</code> is <code>application/vnd.kidspiration</code>	
.odf	A											The MIME type for the extension <code>.odf</code> is <code>application/vnd.oasis.opendocument.formula</code>	
.tra	A											The MIME type for the extension <code>.tra</code> is <code>application/vnd.trueapp</code>	
.fsc	A											The MIME type for the extension <code>.fsc</code> is <code>application/vnd.fsc.weblaunch</code>	
.ifm	A											The MIME type for the extension <code>.ifm</code> is <code>application/vnd.shana.informed.formdata</code>	
.sgf	A											The MIME type for the extension <code>.sgf</code> is <code>application/x-go-sgf</code>	
.mime	A											The MIME type for the extension <code>.mime</code> is <code>message/rfc822</code>	
.sgl	A											The MIME type for the extension <code>.sgl</code> is <code>application/vnd.stardivision.writer-global</code>	
.sgm	A											The MIME type for the extension <code>.sgm</code> is <code>text/sgml</code>	
.musicxml	A											The MIME type for the extension <code>.musicxml</code> is <code>application/vnd.recordare.musicxml+xml</code>	
.lbd	A											The MIME type for the extension <code>.lbd</code> is <code>application/vnd.llamagraphics.life-balance.desktop</code>	
.lbe	A											The MIME type for the extension <code>.lbe</code> is <code>application/vnd.llamagraphics.life-balance.exchange+xml</code>	
.maker	A											The MIME type for the extension <code>.maker</code> is <code>application/x-maker</code>	
.fst	A											The MIME type for the extension <code>.fst</code> is <code>image/vnd.fst</code>	
.wm	A											The MIME type for the extension <code>.wm</code> is <code>video/x-ms-wm</code>	
.tif	A											The MIME type for the extension <code>.tif</code> is <code>image/tiff</code>	
.otp	A											The MIME type for the extension <code>.otp</code> is <code>application/vnd.oasis.opendocument.presentation-template</code>	
.dv	A											The MIME type for the extension <code>.dv</code> is <code>video/dv</code>	
.qwt	A											The MIME type for the extension <code>.qwt</code> is <code>application/vnd.quark.quarkxpress</code>	
.wp5	A											The MIME type for the extension <code>.wp5</code> is <code>application/wordperfect5.1</code>	
.f77	A											The MIME type for the extension <code>.f77</code> is <code>text/x-fortran</code>	
.%	A											The MIME type for the extension <code>.%</code> is <code>application/x-trash</code>	
.cache	A											The MIME type for the extension <code>.cache</code> is <code>chemical/x-cache</code>	
.mvb	A											The MIME type for the extension <code>.mvb</code> is <code>chemical/x-mopac-vib</code>	
.les	A											The MIME type for the extension <code>.les</code> is <code>application/vnd.hhe.lesson-player</code>	
.rms	A											The MIME type for the extension <code>.rms</code> is <code>application/vnd.jcp.javame.midlet-rms</code>	
.wk	A											The MIME type for the extension <code>.wk</code> is <code>application/x-123</code>	
.acutc	A											The MIME type for the extension <code>.acutc</code> is <code>application/vnd.acucorp</code>	
.rdf	A											The MIME type for the extension <code>.rdf</code> is <code>application/rdf+xml</code>	
.xsl	A											The MIME type for the extension <code>.xsl</code> is <code>application/xml</code>	
.sxw	A											The MIME type for the extension <code>.sxw</code> is <code>application/vnd.sun.xml.writer</code>	
.spot	A											The MIME type for the extension <code>.spot</code> is <code>text/vnd.in3d.spot</code>	
.nc	A											The MIME type for the extension <code>.nc</code> is <code>application/x-netcdf</code>	
.nb	A											The MIME type for the extension <code>.nb</code> is <code>application/mathematica</code>	
.sxm	A											The MIME type for the extension <code>.sxm</code> is <code>application/vnd.sun.xml.math</code>	
.mseq	A											The MIME type for the extension <code>.mseq</code> is <code>application/vnd.mseq</code>	
.sxi	A											The MIME type for the extension <code>.sxi</code> is <code>application/vnd.sun.xml.impress</code>	
.bib	A											The MIME type for the extension <code>.bib</code> is <code>text/x-bibtex</code>	
.aep	A											The MIME type for the extension <code>.aep</code> is <code>application/vnd.audiograph</code>	
.bin	A											The MIME type for the extension <code>.bin</code> is <code>application/octet-stream</code>	
.rdz	A											The MIME type for the extension <code>.rdz</code> is <code>application/vnd.data-vision.rdz</code>	
.pcf.Z	A											The MIME type for the extension <code>.pcf.Z</code> is <code>application/x-font</code>	
.uris	A											The MIME type for the extension <code>.uris</code> is <code>text/uri-list</code>	
.sxc	A											The MIME type for the extension <code>.sxc</code> is <code>application/vnd.sun.xml.calc</code>	
.h	A											The MIME type for the extension <code>.h</code> is <code>text/x-chdr</code>	
.pptm	A											The MIME type for the extension <code>.pptm</code> is <code>application/vnd.ms-powerpoint.presentation.macroenabled.12</code>	
.itp	A											The MIME type for the extension <code>.itp</code> is <code>application/vnd.shana.informed.formtemplate</code>	
.xltm	A											The MIME type for the extension <code>.xltm</code> is <code>application/vnd.ms-excel.template.macroenabled.12</code>	
.o	A											The MIME type for the extension <code>.o</code> is <code>application/x-object</code>	
.a	A											The MIME type for the extension <code>.a</code> is <code>application/octet-stream</code>	
.b	A											The MIME type for the extension <code>.b</code> is <code>chemical/x-molconn-Z</code>	
.c	A											The MIME type for the extension <code>.c</code> is <code>text/x-csrc</code>	
.rcprofile	A											The MIME type for the extension <code>.rcprofile</code> is <code>application/vnd.ipunplugged.rcprofile</code>	
.udeb	A											The MIME type for the extension <code>.udeb</code> is <code>application/x-debian-package</code>	
.f	A											The MIME type for the extension <code>.f</code> is <code>text/x-fortran</code>	
.fe_launch	A											The MIME type for the extension <code>.fe_launch</code> is <code>application/vnd.denovo.fcselayout-link</code>	
.vtu	A											The MIME type for the extension <code>.vtu</code> is <code>model/vnd.vtu</code>	
.xltx	A											The MIME type for the extension <code>.xltx</code> is <code>application/vnd.openxmlformats-officedocument.spreadsheetml.template</code>	
.aw	A											The MIME type for the extension <code>.aw</code> is <code>application/applixware</code>	
.~	A											The MIME type for the extension <code>.~</code> is <code>application/x-trash</code>	
.au	A											The MIME type for the extension <code>.au</code> is <code>audio/basic</code>	
.p	A											The MIME type for the extension <code>.p</code> is <code>text/x-pascal</code>	
.s	A											The MIME type for the extension <code>.s</code> is <code>text/x-asm</code>	
.t	A											The MIME type for the extension <code>.t</code> is <code>text/troff</code>	
.wpl	A											The MIME type for the extension <code>.wpl</code> is <code>application/vnd.ms-wpl</code>	
.cpp	A											The MIME type for the extension <code>.cpp</code> is <code>text/x-c++src</code>	
.xenc	A											The MIME type for the extension <code>.xenc</code> is <code>application/xenc+xml</code>	
.btif	A											The MIME type for the extension <code>.btif</code> is <code>image/prs.btif</code>	
.gnumeric	A											The MIME type for the extension <code>.gnumeric</code> is <code>application/x-gnumeric</code>	
.mpega	A											The MIME type for the extension <code>.mpega</code> is <code>audio/mpeg</code>	
.pct	A											The MIME type for the extension <code>.pct</code> is <code>image/x-pict</code>	
.docm	A											The MIME type for the extension <code>.docm</code> is <code>application/vnd.ms-word.document.macroenabled.12</code>	
.nnw	A											The MIME type for the extension <code>.nnw</code> is <code>application/vnd.noblenet-web</code>	
.xvml	A											The MIME type for the extension <code>.xvml</code> is <code>application/xv+xml</code>	
.x3d	A											The MIME type for the extension <code>.x3d</code> is <code>application/vnd.hzn-3d-crossword</code>	
.xap	A											The MIME type for the extension <code>.xap</code> is <code>application/x-silverlight-app</code>	
.portpkg	A											The MIME type for the extension <code>.portpkg</code> is <code>application/vnd.macports.portpkg</code>	
.sldx	A											The MIME type for the extension <code>.sldx</code> is <code>application/vnd.openxmlformats-officedocument.presentationml.slide</code>	
.docx	A											The MIME type for the extension <code>.docx</code> is <code>application/vnd.openxmlformats-officedocument.wordprocessingml.document</code>	
.zmm	A											The MIME type for the extension <code>.zmm</code> is <code>application/vnd.handheld-entertainment+xml</code>	
.sv4crc	A											The MIME type for the extension <code>.sv4crc</code> is <code>application/x-sv4crc</code>	
.in	A											The MIME type for the extension <code>.in</code> is <code>text/plain</code>	
.cmc	A											The MIME type for the extension <code>.cmc</code> is <code>application/vnd.cosmocaller</code>	
.pfm	A											The MIME type for the extension <code>.pfm</code> is <code>application/x-font-type1</code>	
.cml	A											The MIME type for the extension <code>.cml</code> is <code>chemical/x-cml</code>	
.m4u	A											The MIME type for the extension <code>.m4u</code> is <code>video/vnd.mpegurl</code>	
.pvb	A											The MIME type for the extension <code>.pvb</code> is <code>application/vnd.3gpp.pic-bw-var</code>	
.jpeg	A											The MIME type for the extension <code>.jpeg</code> is <code>image/jpeg</code>	
.mgz	A											The MIME type for the extension <code>.mgz</code> is <code>application/vnd.proteus.magazine</code>	
.mid	A											The MIME type for the extension <code>.mid</code> is <code>audio/midi</code>	
.mif	A											The MIME type for the extension <code>.mif</code> is <code>application/x-mif</code>	
.chat	A											The MIME type for the extension <code>.chat</code> is <code>application/x-chat</code>	
.cmx	A											The MIME type for the extension <code>.cmx</code> is <code>image/x-cmx</code>	
.mml	A											The MIME type for the extension <code>.mml</code> is <code>text/mathml</code>	
.kml	A											The MIME type for the extension <code>.kml</code> is <code>application/vnd.google-earth.kml+xml</code>	
.dump	A											The MIME type for the extension <code>.dump</code> is <code>application/octet-stream</code>	
.gph	A											The MIME type for the extension <code>.gph</code> is <code>application/vnd.flographit</code>	
.xht	A											The MIME type for the extension <code>.xht</code> is <code>application/xhtml+xml</code>	
.dif	A											The MIME type for the extension <code>.dif</code> is <code>video/dv</code>	
.sfd-hdstx	A											The MIME type for the extension <code>.sfd-hdstx</code> is <code>application/vnd.hydrostatix.sof-data</code>	
.wz	A											The MIME type for the extension <code>.wz</code> is <code>application/x-wingz</code>	
.ncx	A											The MIME type for the extension <code>.ncx</code> is <code>application/x-dtbncx+xml</code>	
.m3u	A											The MIME type for the extension <code>.m3u</code> is <code>audio/x-mpegurl</code>	
.kmz	A											The MIME type for the extension <code>.kmz</code> is <code>application/vnd.google-earth.kmz</code>	
.gpt	A											The MIME type for the extension <code>.gpt</code> is <code>chemical/x-mopac-graph</code>	
.mp2	A											The MIME type for the extension <code>.mp2</code> is <code>audio/mpeg</code>	
.x32	A											The MIME type for the extension <code>.x32</code> is <code>application/x-authorware-bin</code>	
.teacher	A											The MIME type for the extension <code>.teacher</code> is <code>application/vnd.smart.teacher</code>	
.dis	A											The MIME type for the extension <code>.dis</code> is <code>application/vnd.mobius.dis</code>	
.vst	A											The MIME type for the extension <code>.vst</code> is <code>application/vnd.visio</code>	
.curl	A											The MIME type for the extension <code>.curl</code> is <code>text/vnd.curl</code>	
.setpay	A											The MIME type for the extension <code>.setpay</code> is <code>application/set-payment-initiation</code>	
.cod	A											The MIME type for the extension <code>.cod</code> is <code>application/vnd.rim.cod</code>	
.vmd	A											The MIME type for the extension <code>.vmd</code> is <code>chemical/x-vmd</code>	
.w3d	A											The MIME type for the extension <code>.w3d</code> is <code>application/x-director</code>	
.flac	A											The MIME type for the extension <code>.flac</code> is <code>application/x-flac</code>	
.snd	A											The MIME type for the extension <code>.snd</code> is <code>audio/basic</code>	
.mmd	A											The MIME type for the extension <code>.mmd</code> is <code>chemical/x-macromodel-input</code>	
.mjp2	A											The MIME type for the extension <code>.mjp2</code> is <code>video/mj2</code>	
.jpgv	A											The MIME type for the extension <code>.jpgv</code> is <code>video/jpeg</code>	
.ftc	A											The MIME type for the extension <code>.ftc</code> is <code>application/vnd.fluxtime.clip</code>	
.pqa	A											The MIME type for the extension <code>.pqa</code> is <code>application/vnd.palm</code>	
.mcm	A											The MIME type for the extension <code>.mcm</code> is <code>chemical/x-macmolecule</code>	
.vms	A											The MIME type for the extension <code>.vms</code> is <code>chemical/x-vamas-iso14976</code>	
.fti	A											The MIME type for the extension <code>.fti</code> is <code>application/vnd.anser-web-funds-transfer-initiation</code>	
